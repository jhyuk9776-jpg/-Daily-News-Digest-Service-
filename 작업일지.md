# 작업일지

데일리 뉴스 다이제스트 프로젝트의 날짜별 작업 기록.
**목적:** 서비스 완성 후 하나의 기획서로 엮기 위한 재료. 그래서 "무엇을 했는지"뿐 아니라
**"왜 그렇게 결정했는지(고민한 배경과 트레이드오프)"**를 함께 남긴다.
각 일자를 **원래 일정(계획)** 과 **실제 작업(한 것)** 으로 나눠 기록한다.

## 진행 개요

로드맵은 Day 1~5(하루 1단계)로 계획했으나, 실제로는 3일에 압축해 진행했다.

| 실제 작업일 | 로드맵 대응 | 핵심 |
|---|---|---|
| 1일차 · 2026-06-29 | Day 1 | 기획 확정 |
| 2일차 · 2026-06-30 | Day 2·3·4 | 수집 → 선별/중복제거 → AI 요약/마크다운 (파이프라인 완성) |
| 3일차 · 2026-07-01 | Day 5 | 테스트·자동화 + Phase 2 첫 기능 설계 |

---

## 1일차 — 2026-06-29 (로드맵 Day 1: 기획 확정)

### 원래 일정
- Phase 1 범위 확정: 분야, 정치 포함 여부, 기사 수, 비용 상한, 저장 위치 결정
- AI 요약 비용 가정과 첫 달 예산 설정
- RSS 출처 후보와 다음 구현 파일 목록 정리

### 실제 작업
- 프로젝트 기획 문서 세트 작성: `AI_CONTEXT.md`, `기획/01~05`(product-brief, mvp-spec,
  system-architecture, decision-log, roadmap), `기획/회의내용정리/01-today-plan-and-cost.md`
- 구현은 시작하지 않고 **범위를 선명하게 고정하는 데 집중**.

### 주요 결정과 그 이유 (고민한 지점)

- **"판단 없는 사실 중심" 다이제스트로 방향을 잡음**
  - 고민: 뉴스 서비스는 요약·논평·전망까지 하기 쉬운데, 그러면 AI의 판단이 개입된다.
  - 결정 근거: 타깃 사용자는 "뉴스를 많이 읽고 싶은 사람"이 아니라 "세상의 흐름을
    효율적으로 파악하려는 사람". 그래서 **요약은 판단을 대신 내리지 않고 판단할 재료를
    정리**하는 것으로 원칙을 세움. 평가어("논란이 커지고 있다" 등)를 의도적으로 배제.

- **정치 분야 Phase 1 제외**
  - 고민: 정치는 트래픽이 크지만 논쟁·판단을 유발한다.
  - 결정 근거: 사실 중심 원칙과 충돌하고 초기 범위를 키운다. 제외하되 추후 옵션 처리 여지만 남김.

- **하루 8건(분야별 2건), 저장은 `News/YYYY-MM-DD.md`**
  - 고민: 몇 건이 "충분하되 부담 없는" 양인가.
  - 결정 근거: **읽는 시간 5분** 기준. 많이 주기보다 매일 끝까지 읽게 만드는 게 우선.
    결과물은 웹/DB보다 먼저 **파일 하나로 매일 쓸 수 있게**(작게 시작 원칙).

- **신뢰성 > 요약 품질**
  - 결정 근거: 요약이 매끄러운 것보다 **출처·날짜·중복 제거의 신뢰성**이 먼저다.
    그래서 "출처 링크는 반드시 남긴다"를 하드 원칙으로 둠.

- **로컬 Python + 무료 RSS, 자동화는 GitHub Actions 후보**
  - 고민: 비용을 어디에 쓸 것인가.
  - 결정 근거: 무료 RSS·로컬 실행으로 고정비를 0에 가깝게 두고 **비용은 AI 요약 API에만
    집중**. 자동화는 무료이고 전원/PC와 무관한 GitHub Actions를 우선 검토.

- **웹/앱 스택 TypeScript(Next.js + Expo) 확정 — 단, Phase 3로 미룸**
  - 결정 근거: 언어 하나로 웹+iOS+안드로이드를 커버해 **솔로 개발 부담이 가장 작다**.
    Supabase JS SDK 지원이 두터워 Phase 2 DB와 직결.
  - 미룬 이유: **데이터 파이프라인을 먼저 안정화**한다는 우선순위 원칙. 지금은 기록만.

- **비용: 첫 달 22달러 (Claude Code 월 구독 기준)** — 비용 늘어나는 선택은 먼저 가정을 밝힌다는 규칙.

### 산출물
- 기획 문서 8종, 결정 로그, 로드맵

### 계획 대비
- 계획과 거의 일치. 구현 대신 **기획을 탄탄히 해 이후 3단계를 하루에 완주할 토대**를 만듦.

---

## 2일차 — 2026-06-30 (로드맵 Day 2·3·4 압축: 수집 → 선별 → 요약)

### 원래 일정 (로드맵 기준 3일 분량)
- Day 2: 수집 기능 구현 — 고정 소스에서 기사 리스트 수집
- Day 3: 선별/중복 제거 구현 — 분야별 후보 기사 정리
- Day 4: AI 요약/마크다운 생성 — 날짜별 다이제스트 파일 생성

### 실제 작업 (하루에 3단계 완주 → 파이프라인 end-to-end 완성)
1. **수집(`src/fetch.py`)** — `sources.yaml`의 분야별 RSS를 `feedparser`로 수집,
   발행시각 ISO 정규화, `raw/YYYY-MM-DD.json` 저장. 한 출처 실패가 전체를 막지 않게 예외 격리.
2. **선별/중복제거(`src/select.py` → 이후 `curate.py`)** — 날짜 필터, 증거점수 휴리스틱,
   제목 유사도 클러스터링(같은 사건 묶기)+교차검증 매체 수 집계, 분야별 상위 2건.
3. **AI 요약/마크다운(`src/extract.py`, `src/summarize.py`)** — 본문 확보 후 Claude Haiku
   4.5로 사실 1~3문장 요약(기사별 개별 호출)+URL 캐시, 출처 1:1 부착, `News/2026-06-30.md` 생성.
- 각 단계마다 설계 메모를 먼저 남기고 구현: `docs/superpowers/specs/2026-06-30-day2~4-*.md`

### 주요 결정과 그 이유 (고민한 지점)

- **수집 단계에서 하드 필터를 하지 않고 "넓게 수집 → 선별에서 랭킹"으로 감** (증거 기반 품질 전략)
  - 고민: 처음엔 "증거(숫자·통계) 있는 기사만 남기자"는 하드 필터를 생각했다.
  - 문제 발견: 증거는 통계만이 아니라 **판결·정책 발표·공식 발언**도 포함한다. 하드 필터를
    걸면 중요한 뉴스를 버리고 **분야별 2건을 못 채울 위험**이 있다.
  - 결정: 수집은 넓게 하고, 증거·교차검증을 **선별 단계의 순위 신호(가점)**로만 쓴다.

- **교차검증을 신뢰 신호 + 중복 제거에 함께 사용**
  - 결정 근거: 2개 이상 독립 매체가 같은 사실을 보도하면 **신뢰도가 올라가고**, 동시에
    같은 사건을 하나로 묶어 **중복도 줄인다**. 한 로직으로 두 목적을 달성.

- **요약 모델로 Claude Haiku 4.5 채택**
  - 고민: 1일차엔 "OpenAI 저비용 모델 우선"이었다.
  - 결정 근거: 하루 8건·고정 형식 요약이라 **저비용 소형 모델로 충분**(월 ~$1 예상).
    품질/비용 균형에서 Haiku를 택함.

- **API를 Anthropic 콘솔이 아닌 Replicate 경유로 호출** (실무에서 막혀 우회한 지점)
  - 고민: Anthropic 콘솔 결제가 **카드/본인 검증 문제로 막혔다**.
  - 결정: 같은 모델(`anthropic/claude-4.5-haiku`)을 **Replicate 공식 채널**로 사용.
    제약(최소 max_tokens, 잔액 $5 미만 시 분당 6건 제한)은 429 시 대기·재시도로 흡수.

- **본문 추출은 "빈/짧은 요약만" 보강, 요약은 기사별 개별 호출**
  - 결정 근거: 대상이 **최종 8건뿐**이라 본문 추출·개별 호출의 비용이 거의 0.
    개별 호출은 **격리성·출처 1:1 정확성·URL 캐시** 이점이 커서 선택.

- **"출처 없는 사실은 싣지 않는다"를 요약 단계 원칙으로 못박음** — 신뢰성 우선(1일차 원칙)의 구현.

### 산출물
- `src/fetch.py`, `src/curate.py`, `src/extract.py`, `src/summarize.py`
- `sources.yaml`, `requirements.txt`, `.env.example`, `.gitignore`
- **첫 실제 다이제스트: `News/2026-06-30.md`** (요약 실패 0 · 추출 실패 0)

### 계획 대비
- 로드맵 3일치(Day 2·3·4)를 **하루에 완주**. 설계 메모를 먼저 남기는 방식으로 속도를 냄.
- 예상 못 한 변수(Anthropic 결제 차단)를 Replicate 우회로 해결한 게 이날의 핵심 판단.

---

## 3일차 — 2026-07-01 (로드맵 Day 5: 테스트·자동화)

### 원래 일정
- 실제 실행 결과 검수 및 자동 실행(GitHub Actions) 연결

### 실제 작업
- 요약 폴백 버그 수정, 선별 정렬에서 증거점수 제거(교차검증→매체우선순위→최신순),
  1커맨드 실행(`run.sh`), GitHub Actions cron(07:00 KST) + 자동커밋 설정, 결정 로그 1.3 확정.
  (커밋 `0661d0e`·`7206ebc`·`8a00260`)
- 자동화가 실제로 도는지 수동 트리거로 검증 → 숨은 품질 붕괴 발견·차단 →
  토큰 갱신 재검증 → 다이제스트 검수 → 감점 예시 기록 → 신규 기능 로드맵 반영.

### 주요 결정과 그 이유 (고민한 지점)

- **선별 정렬에서 증거점수를 뺌** (기준을 바꾼 판단)
  - 문제 발견: 증거점수가 최대 5점에 몰려 변별력이 약하고, **감정적 제목도 숫자만 있으면 고득점**.
  - 결정: 랭킹에서 증거점수를 빼고 교차검증수→매체우선순위→최신순으로. 증거점수는 기록용
    메타데이터로만 남김. 객관성 반영은 데이터를 모아 Phase 2에서 제대로 만든다.

- **요약 폴백을 체인으로 설계** (조용한 실패를 막는 방어선)
  - 문제 발견: 짧은 RSS(102자)가 길이 기준을 통과해 부실 요약 → 모델이 대화체 응답 →
    불릿 0개 → 비결정적 실패(매일경제 기사).
  - 결정: 프롬프트를 강화("불릿만, 낼 사실 없으면 아무것도 출력 안 함")하고, 불릿 0개면
    전체 본문 재요약 → 같은 사건 다음 우선순위 매체 본문으로 순차 폴백. `tests/test_fallback.py`로 고정.

- **자동화는 "초록불"이 아니라 "결과물"로 검증해야 한다** (오늘의 핵심 교훈)
  - 고민: 워크플로우가 성공(초록불)으로 떠서 끝난 줄 알았으나, 생성물을 열어보니
    **8건 요약이 전량 실패**하고 링크-only로 폴백돼 있었다. 원인은 Replicate **토큰 401**.
  - 뿌리 원인: 링크-only 폴백은 *개별 기사* 최후 수단인데, *전량 인증 실패*까지 성공으로
    넘겨 **부실 다이제스트가 그대로 main에 자동커밋**됐다. 초록불이 품질 실패를 가림.
  - 결정: `summarize.run()`에 **요약 과반 실패(FAIL_RATIO 0.5) 시 exit 1** 가드 추가.
    파일은 남기되(아티팩트 확인용) `run.sh`의 `set -e`가 자동커밋 단계를 막아 오염 차단.
    테스트 4건 추가. (커밋 `03b7ff1`)
  - 검증: 토큰 무효 상태에서 실제로 CI 빨간불·커밋 차단됨을 확인 → 토큰 갱신 후 재실행에서
    **요약 8건·실패 0**으로 통과·자동커밋까지 확인. (가드가 없었다면 오늘도 쓰레기가 커밋될 뻔)

- **다이제스트 검수에서 "감점 예시 1호" 확보** (Phase 2 연료)
  - 발견: IT/테크 2번 JB금융 ESG 기사가 제목 "성과 입증"(홍보성 평가어)·분야 부적합인데
    숫자가 있어 선별을 통과. 로드맵 6절 객관성 점수가 겨냥한 바로 그 케이스.
  - 결정: 결정 로그 1.3에 감점 예시 1호로 축적. 라벨 데이터가 Phase 2 튜닝 연료가 된다.

- **"분야별 기사 수 사용자 설정" 아이디어를 상한 방식으로 로드맵에 넣음**
  - 아이디어: 분야별 2건 고정을 사용자가 최대 10건까지 조정(4분야면 최대 40건).
  - 개선 반영: **고정 개수가 아니라 상한**으로 두고, 일정 점수(교차검증·객관성) 이상만 요약해
    수가 모자라도 부실 기사를 억지로 채우지 않는다. 상한을 키울 때 품질이 희석되는 최대 단점을
    Phase 2 객관성 점수와 **세트로** 방어. 사용자별 저장은 Supabase(Phase 2), UI는 Phase 3.

### 산출물
- `run.sh`, `.github/workflows/daily.yml`, `tests/test_fallback.py`(10건)
- 요약 과반 실패 가드(`src/summarize.py`)
- **자동 생성·커밋된 다이제스트: `News/2026-07-01.md`** (요약 8건·실패 0)
- 결정 로그 감점 예시 1호, 로드맵 Phase 2 후보 "분야별 기사 수 사용자 설정" 추가

### 계획 대비
- 계획(검수 + 자동화 연결)을 완수하고, **계획에 없던 자동화 신뢰성 구멍(초록불이 가린 전량
  실패)을 발견·차단**한 것이 이날의 핵심. "자동화는 초록불이 아니라 산출물로 검증한다"는
  원칙을 실제 사고로 체득함. **Phase 1 파이프라인·자동화 완료.**

### Phase 2 첫 기능 설계 (객관성 스코어러)

Phase 1을 마치고, Phase 2 첫 기능을 브레인스토밍→설계→구현계획까지 확정(코딩은 다음 세션).

- **아이디어(사용자 제안):** 요약한 8건뿐 아니라 **수집·필터링한 기사 전량**의 점수를 매겨
  매체 객관성 점수를 빨리 안정화하자. 요약은 안 하고 확인만 → 데이터량 ~100배, 비용 0.
- **주요 결정과 이유:**
  - *감점 중심 채점* — 오늘 잡은 JB금융(숫자 있지만 홍보성)이 evidence 가점으로 고득점한
    사례가 근거. 가점과 객관성은 다른 축이라, 객관성은 평가어·홍보성 **감점**으로만 움직이게 함.
  - *기사별 채점 → 매체 EWMA 누적* — 한 기사가 매체 전체를 흔들지 않게 느린 평판(α=0.1).
  - *observe-only* — 선별·랭킹에 반영 안 하고 기록만. 분포가 쌓이면 임계값을 데이터로 결정.
  - *파이프라인과 분리한 수동 실행(CLI+전용 스킬)* — 요약/CI에 안 붙임. CI 러너 초기화로 인한
    누적 소실 문제를 원천 회피하고, `scores/`를 git 추적해 커밋으로 누적.
  - *감점 사전은 고정밀 시드만* — "충격" 같은 단일 모호어는 오탐(지진 충격) 위험으로 제외.
  - *백필 날짜기준 = 파일 날짜* — 나중에 감점 사전 넓혀 재백필해도 과거 기사가 안 걸러지게(보험).
- **산출물:** 설계 `docs/superpowers/specs/2026-07-01-objectivity-scorer-design.md`,
  구현계획 `docs/superpowers/plans/2026-07-01-objectivity-scorer.md`(5태스크 TDD).
- **다음 세션:** 서브에이전트 주도로 계획 실행(Task 1→5).

## 4일차 — 2026-07-02 (Phase 2: 객관성 스코어러 + 분야별 상한 + 실패 처리)

### 원래 일정
- 어제 확정한 구현계획(5태스크 TDD)에 따라 매체 객관성 점수 축적기 구현.

### 실제 작업
- `src/objectivity.py` + `tests/test_objectivity.py`를 **TDD(Red→Green→커밋)로 5태스크 완주**.
  기사별 감점 채점 → 매체 EWMA 집계(멱등) → 저장 I/O → 하루처리·백필·CLI → 전용 스킬+scores git 추적.
  (커밋 `8860ae0`·`ec7d1b9`·`f44a4ae`·`c6acc59`·`d851333`)
- `--backfill`로 기존 raw 2일치 채점 → `scores/media.json` 생성(매체 8곳). 전체 테스트 23건 통과.
- 서브에이전트 대신 **직접 TDD로 진행**하기로 변경 — 파일 하나 규모라 병렬화 이득이 없고,
  브리핑에서 이미 잡은 맥락을 그대로 쓰는 게 서브에이전트 재초기화보다 싸고 투명해서.

### 주요 결정과 그 이유 (고민한 지점)

- **계획서 최소구현의 FLOOR 버그를 테스트가 잡아냄** (TDD가 실제로 일한 지점)
  - 문제: `test_floor_clamp`가 실패. 최소구현은 `phrase in text`(포함 여부)로 감점을 1회만 세서,
    사전 5개·PENALTY=10이면 최대 50점 감점 → **FLOOR(0)에 도달 불가**. 클램프가 죽은 코드였음.
  - 판단: 스펙 §4 "매칭마다 감점"에 맞춰 **출현 횟수 카운트**(`text.count`, `finditer`)로 수정.
    평가어가 반복될수록 더 깎여 FLOOR가 의미를 갖고, 나머지 테스트(1회 등장)도 그대로 통과.
  - 교훈: 테스트를 먼저 돌려보지 않았으면 도달 불가능한 클램프를 모른 채 넘어갔을 것.

- **백필 결과가 대부분 100점** (관찰 단계의 정상 신호)
  - 고정밀 시드 사전이라 RSS 제목에 정확한 다어절 평가어가 거의 없어 감점 표본이 희소(매일경제 1건).
  - observe-only라 이대로 정상. 감점 사전 확장은 축적된 `articles-*.json`을 데이터로 보고 결정.

### 산출물
- `src/objectivity.py`, `tests/test_objectivity.py`(13건), `.claude/skills/objectivity-score/SKILL.md`
- `scores/media.json` + `scores/articles-{2026-06-30,2026-07-01}.json`(git 추적)
- `.gitignore` scores/ 추적 예외 주석

- **제외 매체가 백필에 되살아나는 문제 정리** (검수에서 발견)
  - 발견: 백필 결과에 결정 로그 1.3에서 제외한 "이코노미스트 타임스"가 잡힘(제외 이전 과거 raw 잔존).
  - 결정: `active_sources()`로 **sources.yaml에 있는 활성 매체만 채점**. 재백필해도 옛 출처가
    다시 안 잡히는 방어선. TDD로 필터 테스트 추가 후 수정, scores/ 재구축(매체 7곳). (커밋에 포함)

### 이어서: daily 파이프라인 검증 + 로컬/원격 정렬
- 07-02 자동 실행(cron)이 정상 동작해 07-02 다이제스트가 원격에 커밋됨을 확인(초록불이 아니라 산출물로).
- 로컬이 원격과 갈라져 있어 `git pull --rebase`로 정렬(원격 다이제스트 위에 로컬 작업 얹음).

### 분야별 요약 상한 (Phase 1.5 선행판) — brainstorm→spec→plan→TDD 완주
- 아이디어: "경제만 10건으로 늘려 파이프라인이 상한까지 도는지 테스트"(사용자).
- 설계 판단: 품질 게이트가 붙은 완성형은 객관성 점수가 랭킹에 붙는 Phase 2 본편. 지금은 로드맵이
  예고한 **"값만 바꾸는 전역 N 선행판(Phase 1.5)"** 으로 분리 — 계획을 어기는 게 아니라 명시된 옵션 사용.
- 구현: `limits.yaml`(default+per_category) + `curate.load_limits` + `select` 분야별 상한. `PER_CATEGORY` 제거.
- 검증: 경제=10 실제 실행 → 경제 10건 요약·종료 0·가드 미발동, `limits.yaml` 없으면 2건 폴백(하위호환).
- 배운 점: RSS 요약 빈 값은 한국경제의 **정상**(9/10건이 빈 값이나 8건 정상 요약). "RSS 없으면 제외"는
  좋은 기사(무역수지 흑자 등)를 버리는 과잉 규칙이라 반려하고, 판정 시점을 **폴백 끝(최종 0불릿)** 으로 옮김.

### 실패 처리: 제외 + 실패 로그 + 알림 — brainstorm→spec→plan→TDD 완주
- 계기: 검수에서 한국경제 칼럼이 요약 실패(본문 추출이 관련기사 목록·광고를 긁음 → 모델이 사실 0). 링크-only 잔재 발견.
- 결정: (1) 사실 못 뽑은 기사는 다이제스트에서 **제외**(빈자리 안 채움=A안, B 백필은 복잡도·하위후보 품질·비결정성으로 반려)
  (2) 실패를 `failures/failures-YYYY-MM-DD.json`에 **원인별 축적**(api_failed/call_error/extract_failed) — 실패는 버릴 게 아니라 자산
  (3) 실패 시 **CLI 알림**(실시간 푸시는 알림 채널 없어 Phase 2).
- 함정 2개를 계획에 못박음: `summarize_item` 4→5튜플 확장으로 **기존 테스트 전부 갱신**,
  401 대량 인증실패가 call_error로 분류돼도 **FAIL_RATIO 가드 유지되게 call_error 포함**.
- 반려한 확장: "노이즈성 이벤트(429 등)를 매체 감점으로" — 429는 매체 무관 전역 제한이고, 추출 난이도(기술 축)를
  객관성(편향 축)에 섞으면 두 신호가 오염됨. 실패 로그 기반 별도 observe-only 후속으로 미룸(사용자도 철회).
- 검증: 경제=10에서 칼럼이 다이제스트에서 빠지고 `failures-2026-07-01.json`에 기록(경제 9건). 실패 0건인 06-30은 파일 미생성.

### 주요 결정과 그 이유 (프로세스 측면)
- **사용자 제안에 반대 이유가 있으면 근거와 함께 되묻는다**는 협업 규칙을 확립(기억에 저장). 이날 "RSS 없으면 제외",
  "노이즈 감점" 두 제안을 데이터·축 분리 근거로 반려해 더 나은 방향으로 수렴.

### 계획 대비
- 하루에 **3개 기능(객관성 스코어러·분야별 상한·실패 처리)** 을 brainstorm→spec→plan→TDD로 완주하고 원격 push까지.
  계획서 버그(FLOOR)를 TDD로 잡고, 검수로 제외 매체 잔존·칼럼 추출 실패를 발견해 정리한 것이 이날의 핵심.
- 남긴 백로그: 본문 추출기의 칼럼 페이지 대응, 실시간 푸시 알림(Phase 2), 실패 로그 기반 매체 추출 신뢰도(observe-only).

### 이어서: 상업화 기획 착수
- **기획을 분야별로 분리**하기로: `기획/{서비스기획, 구현기획, 수익원가, 법무저작권, 운영, …}`.
  일지(시간순)는 원본 유지, 거기서 분야별 기획서를 뽑는 구조.
- **법무·저작권 담당 커스텀 에이전트** 신설(`.claude/agents/legal-copyright.md`) — "AI 직원 고용" 실험의 첫 사례.
  추천은 "지금은 스킬 위주 + 독립 조사형 도메인만 에이전트로"(초기 상호의존 큰 단계라 8개 에이전트는 과설계).
- **서비스 기획서 초안** 작성(`기획/서비스기획/2026-07-02-서비스기획서.md`, SB7+기술설계).
  - 주인공: 30~40대 지식노동자(1차), B/C 확장.
  - 요금: 3티어(Basic/Pro/Max), **총 N + 사용자 배분**. 무료는 총 8 + 배분 커스텀(원가 0), 유료는 볼륨 확대.
  - **핵심 원가 통찰:** 공유 풀+캐시로 무료 한계비용≈0 → 소수 유료(한 자릿수~십수 명)로 흑자.
    사용자별 생성 방식은 무료 유저에 비례해 적자 → 구조적으로 기각.
  - 최대 리스크: 유료 전환 시 저작권/약관(SBS 비상업 조건 등). 법무 에이전트로 조사 예정. 가격 숫자는 미정.

## 5일차 — 2026-07-03 (Phase 2: 감점 사전 등급화 + 객관성 자동 채점)

### 원래 일정
- 아침 브리핑으로 시작해, 상업화 기획(법무/원가) 트랙으로 이어갈 예정이었음.

### 실제 작업 (기획 대신 객관성 채점 품질로 방향 전환)
- **브리핑에 시스템 점검 요구가 추가됨.** 오늘 cron 정상 동작을 산출물로 확인(원격 `93247c0`,
  07-03 디제스트·실패 1건=하입 칼럼 추출실패=알려진 백로그). 브리핑 요청 시 ①정상작동 ②실패(무엇·왜)
  ③당일/누적 점수를 함께 보도록 `daily-briefing` 스킬을 확장.
- **감점 사전 판별력 개선.** 기존은 정확문구 4개뿐이라 거의 발동 안 함(대부분 100점). AI를 **설계 도우미로
  1회** 써서(운영은 결정적 매칭 유지, 비용 0) 실제 표본 54개+원칙으로 **등급 사전** 설계 → `penalties.yaml`.
  - 프롬프트/초안을 `docs/prompts/`에, 설계 스펙을 `docs/superpowers/specs/`에 남김.
  - 오탐 여지(다더니·이러다·극적)는 `observe_candidates`(감점 0·기록만)로 분리.
- **채점 코드 등급화(TDD).** `objectivity.py`에 `load_penalties`(파일 로더·폴백·잘못된 정규식 스킵) +
  등급 weight 합산 감점 + observe 기록 + `penalty_memo`(당일 "무엇·왜·얼마나") 추가. 테스트 45건 통과.
  - `--backfill`로 06-30·07-01 재산정: 감점 1건 → 06-30 6건/07-01 9건으로 판별력 확보. 07-03도 축적(감점 7건).
- **07-03 디제스트 반입 + 점수 축적.** 재요약 대신 원격 발행본을 News/에 가져오고(비용 0), fetch→채점.

### 주요 결정과 그 이유 (고민한 지점)
- **AI-judge(기사별 LLM 채점) 반려, 결정적 사전 확장 채택.** 로드맵 §6.3 "전량 record-only 비용 0"을 지키려
  런타임 AI를 안 씀. AI는 사전 설계 때만 1회 사용.
- **raw 미보관 논쟁 → cron 자동 채점(Option 1)으로 해결.** "수동 채점 놓친 날(07-02)은 raw 소멸로 영영 채점
  불가" 문제. raw 7일 보관도 검토했으나 (히스토리 비대·하루 ~1000건) **cron이 요약 직후 그 raw로 자동 채점하고
  scores만 커밋**하는 쪽이 더 싸고 놓침 0. 대가로 과거 재채점은 포기(사전 크게 바꾸면 `media.json` 리셋).
  결정 로그 1.4에 기록.
- **fetch는 AI 토큰 0**임을 코드로 확인(feedparser HTTP 파싱). 그래서 자동 채점 추가에 추가 비용 없음.

### 산출물
- `penalties.yaml`, `src/objectivity.py`·`tests/test_objectivity.py`(45건), `.github/workflows/daily.yml`(채점 스텝),
  `daily-briefing`·`objectivity-score` 스킬 갱신, 결정 로그 1.4, 설계 스펙·프롬프트 문서, scores 재산정.

### 이어서 / 미검증
- 워크플로 CI 실제 구동은 push 후 `workflow_dispatch` 1회로 최종 확인 필요.
- 감점 사전 오탐 관찰 뒤 가중치·observe 승격 조정. 상업화 기획(법무·원가) 트랙은 여전히 대기.

## 6일차 — 2026-07-04~06 (기획 문서 통합·정리 + 무료 웹 출시로 로드맵 재설계)

### 원래 일정
- 아침 브리핑 후 상업화 기획(법무·원가) 트랙을 이어갈 예정이었음.

### 실제 작업 (트랙 대신 흩어진 기획 문서 정리로 방향 전환)
- **브리핑.** 07-04 시스템 정상 동작 산출물 확인(원격 `News/2026-07-04.md`, 실패 1건=한국경제 프라이스 코너
  불릿 0, 당일 감점 -22). cron 자동 채점 정상.
- **기획 문서 통합.** 문서가 너무 퍼져 있어 정리. Phase 정본을 서비스기획서(07-02)로 정하고,
  낡은 `05-roadmap`·`06-일정`을 **`기획/일정/일정-로드맵.md` 한 파일로 통합**. 초기 기획 `01·02`는
  달성 완료 역사 기록으로 `기획/보관/`으로 이동(삭제 아님, git mv). 참조 10개 문서 일괄 갱신(깨진 링크 0).
- **순번 접두어 정리.** `04-decision-log`→`decision-log`, `보관/01·02`→`product-brief·mvp-spec`
  (기능설계 01~06은 파이프라인 순서라 유지). 사용자 판단으로 범위 한정.
- **로드맵 재설계(무료 웹 출시).** "Phase 3까지 완료해 바로 출시"를 목표로 재설계하되, 대화로 스코프를
  좁힘: **계정·결제·앱·DB 제외 → 무료 웹(PWA)만, 무DB 정적, 07-10 출시(버퍼 ~12)**.

### 주요 결정과 그 이유 (고민한 지점)
- **유료 출시 → 무료 웹 우선.** 최대 병목은 코딩이 아니라 **결제 시 저작권·PG 심사**. 무료 웹으로 자르면
  그 게이트가 출시 후로 밀려 10일→5일 압축 가능. 토스페이먼츠는 **연동 골격도 출시 후**로(과금이 나중이라 실익 낮음).
- **무DB 정적 채택.** 계정이 없으니 사용자별 서버 상태 불필요 → 파이프라인 JSON 출력을 웹이 정적으로 읽음.
  무료 한계비용≈0 통찰과 정합. Supabase·계정은 유료 붙일 때.
- **배분 UI를 경제 한 분야로 선검증.** 기본 16건(경제 10 + 나머지 각 2), 표시 조절은 경제만(기본10·2~10).
  나머지 확대는 유료 때. Phase 1.5 총 N을 좁게 검증.
- **"무료라도 서비스 제공은 공중송신"** 경고를 남김 — 개인 이용과 다르므로 법무 라이트 체크는 유지(07-06 착수).
- **출시 정의 = "도메인만 받으면 되는 상태"**(Vercel 프리뷰까지 완성, 소프트 론칭).

### 산출물
- `기획/일정/일정-로드맵.md`(통합·재설계), `기획/보관/`(01·02 이동), 참조 갱신(AI_CONTEXT·README·
  daily-briefing 스킬·legal 에이전트·시스템기획 문서들), 파일 순번 접두어 정리.

### 이어서
- 07-06: 이메일 발송 구현 + Phase 1.5 16건 파이프라인 + 웹 JSON 계약 확정 + 법무 라이트 체크 착수.

## 7일차 — 2026-07-06~07 (객관성 점수 변별력 "그물망" — 감점 밀도 전환, observe-only)

### 원래 일정
- 새 대화에서 객관성 점수 **변별력 개선**(방향 A 확정) 세부 설계를 이어 brainstorming→plan→구현.

### 실제 작업 (brainstorm→spec→plan→8태스크 TDD→PR 완주)
- **진단 재확인.** `media.json` 7개 매체가 99.8~100에 몰려 변별 불가. 원인은 "감점 1~2%뿐 → 100 평균 →
  EWMA(α=0.1)가 안 움직임". **핵심 통찰: 집계 방식이 진짜 원인**(메모리는 "신호 늘리기"를 1차로 적었으나
  데이터상 집계가 지배적). 감점률은 이미 0~2.8%로 벌어져 있었음.
- **설계(brainstorming).** 집계를 **감점 밀도(1000건당 감점 point, 낮을수록 객관적)**로 전환. 두 트랙
  (트랙1 전량 제목+리드=매체 변별 / 트랙2 다이제스트 본문 감사=격리 모듈). 4신호 ①구조낚시 ②인용밀도
  ③어휘확장 ④교차이상치(②④는 감점에 안 섞는 관찰축). 채널별 채점+차등가중(body_factor)+가속(escalation).
- **구현.** 8태스크 서브에이전트 구동(태스크별 리뷰 + whole-branch 리뷰). `objectivity.py`+`penalties.yaml`
  +신규 `src/digest_audit.py`. 튜너블은 `scoring:` 블록. **69/69 통과.** PR #3 생성.
- **변별력 검증.** backfill(3일) → 밀도 **0.0(한국경제)~388.9(전자신문)/1k**로 벌어짐(압축 해소). 한국경제
  최저=가장 객관적, 기존 직관과 일치.

### 주요 결정과 그 이유 (고민한 지점)
- **집계 우선(방향 A의 올바른 형태).** 사전을 키워도 "100 평균"이면 구조적으로 99점대 압축. 밀도로 바꿔야
  변별이 생김. 밀도는 이진 감점률이 아니라 **감점 point/1000건**으로 정의(등급·가속·차등가중이 살아남게).
- **두 트랙 분리.** 트랙2(본문 포함 16건)를 트랙1 매체 밀도에 섞으면 텍스트량·모집단이 달라 오염 → 별도
  리포트로 격리. 사용자 요구 "본문도 채점"은 트랙2로 수용.
- **트랙2 설계정정(실행 중 발견).** 계획대로면 text scope 감점이 본문을 안 봐서 트랙2가 무의미했음.
  사용자 요구("같은 표현 제목 vs 본문 다른 가중")대로 text 항목이 본문도 body_factor로 채점하게 확장.
- **`…` 오탐 제거(ISSUE-003).** 첫 backfill에서 말줄임표가 60% 오탐(한국 제목 표준부호) → 제거. weight
  튜닝이 아니라 잘못된 신호(버그)라 즉시 수정. 이후 분포가 실제 평가·조롱·낚시 표현 기준으로 재편.
- **observe-only 유지.** curate/summarize/run.sh/daily.yml 불변 → 다이제스트 위험 0. 선별 반영(B안)은
  변별력 확인·데이터 축적 후 별도.

### 실패·오탐 자산화
- **ISSUE-003(품질이슈 로그, 해결):** `…` 대량 오탐 근본원인·조치·잔여 캘리브레이션 후보 기록.
- 잔여 후보: `\?$` weight, 인용밀도가 RSS요약 제공여부에 교란(한국경제 summary 빈값→인용 0), 교차
  이상치가 대부분 단독클러스터로 포화(정의 재검토), 표본 3일로 작음.

### 산출물
- `src/objectivity.py`(밀도 집계·채널채점·관찰축), `src/digest_audit.py`(신규 트랙2), `penalties.yaml`
  (scoring 블록·scope·신호 확장), 스킬 2종 갱신, 테스트 69건, `scores/`(밀도 스키마 재구축).
- 설계 `docs/superpowers/specs/2026-07-06-객관성-변별력-그물망-design.md`, 계획 `.../plans/...`, PR #3.

### 이어서
- PR #3·#2 병합 판단. 병합 후 **선별 반영(B안)** 브레인스토밍(curate 건드림) — PR #2 + 신규 매체 데이터
  며칠 축적 뒤. 상수 캘리브레이션(ISSUE-003 잔여후보)도 데이터 쌓고.

## 8일차 — 2026-07-07 (품질 PR 정리 → 웹 다이제스트 JSON 계약·조회화면, PR #4)

### 원래 일정 (로드맵 07-07)
- Next.js(PWA) 뼈대 + 다이제스트 조회 화면(정적 JSON) + 법무 결론 반영.

### 실제 작업
- **아침 브리핑.** 산출물 기준 상태 점검: 07-07 다이제스트 정상 생성(16건), 요약실패 2건(한국경제 경제,
  "모든 후보 불릿 0"), 당일 감점 -19pt(논란이 커지고 있다/입증/무더기), 누적 media.json이 아직 **구 score
  스키마**(밀도 리셋본은 PR #3에 있음)임을 확인. 로드맵(웹 착수)과 실제(품질 PR 미병합)의 어긋남을 먼저 정리.
- **PR #3 병합**(객관성 밀도 전환). main에 밀도 스키마 반영 → 이후 cron이 새 스키마로 누적.
- **PR #2 병합**(본문추출 정확도 + 매체확장 연합·한겨레). main 변경으로 충돌(문서 `품질이슈-로그.md` 1건)
  → ISSUE-002는 수정본 채택 / CONSTRAINT-001·ISSUE-003 둘 다 보존으로 해소 후 병합. 테스트 81건 OK.
- **추출 개선 실검증.** 오늘 실패한 한국경제 2건 URL에 개선된 추출기 적용 → 본문 1,607자·2,000자(수치 포함)
  정상 추출. 근본원인(추천위젯 오추출→본문0) 해결 확인(비용 0, 요약 API 미호출).
- **웹 작업(신규).** brainstorm→spec→plan→서브에이전트 5태스크 TDD→최종 리뷰→PR #4 완주.
  파이프라인이 `collect_digest`로 마크다운/JSON을 단일 소스에서 파생, `web/public/data/<date>.json`+`latest.json`
  출력. Next.js 15(App Router)+TS 최소 조회화면이 `latest.json` 읽어 분야별 렌더. 파이썬 83·웹 1·빌드 성공.

### 주요 결정과 그 이유 (고민한 지점)
- **웹으로 직행 대신 품질 PR 먼저.** 로드맵은 웹이지만 JSON 계약이 실제 미완이었고 품질 PR 2건이 떠 있었음.
  **#3(채점기준)→#2(매체추가)** 순서로 병합해야 "새 스키마 위에 새 매체"가 얹혀 재작업이 없음.
- **웹 계약 = 단일 소스 파생.** 마크다운을 파싱하는 대신 `collect_digest` 하나에서 마크다운·JSON이 갈라지게
  해 둘의 drift를 구조적으로 차단. `build_markdown` 시그니처는 유지(기존 테스트가 불변 가드).
- **오늘자 1개 + 날짜별 파일 구조.** `latest.json`(최신 복사본)만 웹이 읽어 날짜 하드코딩·index 불필요.
  날짜파일이 쌓여 있어 나중 날짜 네비게이션은 계약 안 깨고 `index.json`만 추가하면 됨.
- **범위 YAGNI.** PWA·경제 개수조절 UI·배포·점수 노출·디자인 폴리시는 로드맵 뒷날로 명시 제외.

### 실패·결함 자산화
- **테스트 격리 결함(병합 전 발견·수정).** Task 2가 `run()`에 JSON 쓰기를 추가했는데, 기존 `test_fallback`이
  `run()`을 호출하며 `WEB_DATA_DIR`를 패치하지 않아 **실제 작업트리를 오염**(커밋된 dev 샘플 latest.json이
  테스트 데이터로 덮임). 최종 whole-branch 리뷰는 "테스트 통과"만 봐서 놓쳤고, 브랜치 마무리 단계의 `git status`
  점검에서 포착. → 해당 테스트에 `WEB_DATA_DIR` 격리 추가, 스위트 후 트리 청결 재검증(commit 2497731).
  **교훈: "테스트 green"과 "테스트가 트리를 안 더럽힘"은 별개다.** run()에 새 IO 추가 시 그걸 호출하는 모든
  기존 테스트의 격리를 함께 점검해야 함(플랜이 이를 명시 못 하면 통합 시점에 샌다).

### 산출물
- `src/summarize.py`(`collect_digest` + JSON 출력 + `WEB_DATA_DIR`), 테스트 `test_collect_digest`·`test_digest_json`,
  `test_fallback` 격리 보강.
- `web/`(Next.js 스캐폴드, `lib/types.ts`, `components/Digest.tsx`+테스트, `app/page.tsx`, dev 샘플 `latest.json`).
- 설계 `docs/superpowers/specs/2026-07-07-웹-다이제스트-조회화면-design.md`, 계획 `.../plans/2026-07-07-...md`.
- 병합: PR #3(밀도 전환)·PR #2(추출·매체확장)·PR #4(웹 JSON 계약·조회화면). 열린 PR 0.

### 이어서
- 로드맵 07-08(M2 웹 코어): 경제 표시개수 조절 UI(기본10·2~10·localStorage) + 관심분야 필터 + 반응형.
- 배포(Vercel)·cron→JSON→웹 자동갱신은 M3(07-09). 그 전에 파이프라인이 매일 `web/public/data`에 커밋하도록
  cron 배선 필요(현재는 dev 샘플만 트래킹).
- 선별 반영(B안)·상수 캘리브레이션은 신규 매체 데이터 며칠 축적 후.

---

## 9일차 — 2026-07-09 (M2 병합 + M3 배선·PWA·법무 + Liquid Glass 디자인 + 순위 이력, PR #5·#6·#7)

### 원래 일정 (로드맵 07-09, M3)
- PWA(매니페스트·SW·오프라인·설치) + 약관·개인정보·소개 페이지 + 배포(Vercel) + cron→JSON→웹 자동 갱신.

### 실제 작업
- **아침 브리핑.** 산출물 기준 점검: 07-09 다이제스트 정상(원격 커밋), 실패 0건, 당일 감점 -109pt(물음표 종결
  낚시 -40·무더기·후폭풍 등), 누적 밀도 1위 한국경제(32.0/1k)·2위 연합뉴스. 로드맵상 M3이지만 **PR #5(웹 M2)·
  #6(기자 블랙리스트)가 미병합**으로 밀려 있음을 확인 → 병합부터 정리하기로.
- **PR #5·#6 병합.** #5(웹 M2 코어: 분야별 개수조절·전체조절·되돌리기·필터) 웹 28건 확인 후 → #6(부실 기자
  블랙리스트) 파이썬 111건 확인 후 순서로 병합. 로컬 main이 원격과 갈라져 있던 것(설계 docs 2커밋 vs 자동
  다이제스트 2커밋) 정리·동기화.
- **M3 배선.** cron이 `web/public/data`(latest.json+날짜별)를 강제 커밋하도록 `daily.yml` 수정. PWA(매니페스트·
  오프라인 SW·설치 메타·아이콘) + `/about`·`/privacy`·`/terms` 라이트 페이지 + 공통 Footer. next build 8/8 정적.
- **사용자 수정 5건.** ① 이메일 카톡용 plain에서 출처 URL 제거(HTML은 링크 유지) ② 웹 '카톡용 복사' 버튼
  (현재 화면 plain, 링크 없음) ③ (설명) 출처가 대표 1개·경제=연합뉴스 쏠림 이유 ④ 개수조절 0~10 드롭다운
  (0=숨김, 체크박스 제거) + 총 표시 건수 ⑤ 브리핑 매체 순위+전일대비 변동 + 일별 순위 이력 누적.
- **Liquid Glass 디자인 개편 + 후속 조정.** 그린 브랜드 팔레트 전면 적용, Tailwind 없이 LiquidButton 순수 CSS
  이식. 이후 사용자 피드백으로 UX 순서(제목→메뉴→본문) 재배치, 컨트롤 sticky 해제, 카톡 복사 톤다운·이동,
  분야명+개수조절을 유리 pill 단일 컴포넌트(`CategoryControl`)로 통합.
- **PR #7 병합.** 위 전부를 `feat/web-m3-deploy-pwa`로 묶어 main 병합. 웹 30·파이썬 114·소스 타입 0.

### 주요 결정과 그 이유 (고민한 지점)
- **병합 순서 #5→#6.** 웹 코어(#5)를 먼저 main에 고정한 뒤 품질 브랜치(#6)를 얹어야 disjoint 영역(웹 vs
  파이프라인) 충돌이 최소. 실제로 clean 병합.
- **cron→웹 데이터는 CI에서만 강제 커밋.** `web/.gitignore`가 data json을 무시(로컬 dev 노이즈 방지)하므로,
  워크플로에서만 `git add -f web/public/data/`. 로컬 청결과 프로덕션 커밋을 동시에 챙김.
- **웹 최신화 = SSG 재빌드.** page가 build-time에 `latest.json`을 읽으므로, cron이 매일 그 파일을 커밋→push→
  Vercel 재빌드→실데이터 반영. 무DB 정적에서 가장 단순한 자동 갱신 경로(서버 상태 0).
- **카톡 원클릭 복사는 웹 전용.** 이메일은 클라이언트가 JS를 막아 복사 버튼이 원천 불가 → 웹에만 배치.
  "이메일→카톡" 워크플로가 "웹→카톡"으로 이동함을 사용자와 확인 후 결정.
- **0=숨김으로 체크박스 통합.** 개수 드롭다운에 0을 넣어 '숨김'과 개수를 한 컨트롤로 → disabled 모델·필터
  체크박스 삭제로 UI·상태 단순화.
- **순위 이력을 매일 스냅샷.** 전일 대비 변동뿐 아니라 "어떤 매체가 1위를 며칠 유지했나"를 이력에서 계산 →
  객관성이 **지속적으로** 높다는 증거로 축적(사용자 제안). raw 미보관이라 소급 불가 → 오늘부터 기준선.
- **Tailwind 도입 대신 순수 CSS 이식.** 붙여준 컴포넌트는 shadcn/Tailwind지만 앱이 작아, 기존 스택 유지가
  가볍고 빠름. 동일 API(`<LiquidButton className>`)로 이식하고 `@/` 별칭만 추가.
- **유리엔 배경이 필요.** glassmorphism은 순백 위에선 안 보임 → 가독성 지키는 옅은 그린 앰비언트 필드를 깔고
  본문 카드는 near-white 프로스트로 대비 유지.
- **UX 순서 제목→메뉴→본문.** 제목으로 서비스를 파악→메뉴로 기능을 짐작→본문을 읽는 인지 흐름(사용자 제안).
  제목을 히어로로 최상단에 올리고 컨트롤을 그 아래로.

### 실패·결함 자산화 (이번 세션 2건)
- **순위 이력 테스트 격리 결함(8일차 패턴 재발).** `run_backfill`이 `update_rank_history`를 호출하는데
  `test_backfill`이 `RANK_HISTORY_FILE`을 패치하지 않아 **실제 `scores/media-rank-history.json`을 오염**
  (테스트의 과거 날짜 `2020-01-15`가 실파일에 유입). 모듈 상수라 `SCORES_DIR` 패치만으로는 안 바뀌어 별도
  패치가 필요했음. 트리 청결 검증에서 포착 → 테스트에 `RANK_HISTORY_FILE` 격리 추가, 파일 재시드로 정리.
  **8일차 `WEB_DATA_DIR` 결함과 동일 유형이 재발** — "새 IO를 호출하는 기존 테스트의 격리를 함께 점검"이라는
  교훈이 아직 습관화 안 됐다는 신호. 새 파일 IO 함수를 만들 때 그 함수 경로 상수를 쓰는 모든 테스트를 즉시 패치.
- **dev 서버와 `.next` 빌드 충돌.** 사용자가 `npm run dev`를 켜둔 상태에서 headless 시각 검증용으로 같은
  `web/.next`에 `rm -rf .next && npm run build`를 돌려 **사용자 dev 서버 캐시를 깨뜨림**(`Cannot find module
  './124.js'` 런타임 에러, 그리고 `reset-btn`↔`liquid-button` 하이드레이션 불일치 — 둘 다 코드 버그가 아니라
  같은 `.next`를 두 프로세스가 두고 다툰 증상). **교훈: 사용자 dev가 도는 동안 같은 `.next`로 빌드/재빌드
  금지.** 이후엔 빌드 대신 **읽기 전용 스크린샷(HTTP GET)** 으로 HMR 반영분만 확인.

### 산출물
- 파이프라인/CI: `daily.yml`(web/public/data 강제 커밋), `notify.py`(카톡 링크 제거), `objectivity.py`
  (`compute_ranks`·`media-rank-history.json`), `daily-briefing` 스킬(순위·변동 출력).
- 웹: `app/manifest.ts`·`public/sw.js`·`ServiceWorkerRegister`·`Footer`·`about/privacy/terms`,
  개수 드롭다운·`totalShown`·`CopyKakaoButton`·`kakaoText`, `ui/liquid-glass-button`(순수 CSS)·그린 팔레트
  `globals.css`·`CategoryControl`, `@/` 별칭(tsconfig·vitest).
- 병합: PR #5(웹 M2)·PR #6(기자 블랙리스트)·PR #7(M3·PWA·법무·디자인·순위이력). 열린 PR 0.

### 이어서
- **Vercel 연결(실데이터 스위치).** 연결되면 매일 cron 커밋→재빌드→실제 요약 기사가 사이트에 반영. 현재는
  의도적으로 dev 샘플(2026-07-07)만 노출.
- 로드맵 M4(07-10): E2E 검수 + **실제 기사로 콘텐츠 QA**.
- 선별 반영(B안)·상수 캘리브레이션은 신규 매체 데이터 며칠 축적 후.

## 10일차 — 2026-07-13~15 (선별재설계 착수 — 대표를 "본문 채점"으로, `feat/selection-redesign`)

### 원래 일정
- 07-10~12는 M4 버퍼·자동실행 구간(수동 dev 커밋 없음, cron 다이제스트만). 이후 선별 품질 재설계 착수.
- 목표: "누가 먼저·많이 썼나"(교차검증수+수동 우선순위)로 뽑던 대표를 "누가 **객관적·근거 있게** 썼나"로 전환.

### 실제 작업 (brainstorm→spec→plan→TDD 구현)
- **채점 엔진(07-13).** `body_objectivity`(제목전용 룰을 `scope:title`로 격리해 본문 채점과 분리),
  `evidence_signals`(숫자·%·기관·인용·기간 신호), 라벨 픽스처(예시 1호·감점 1호)로 채점 기준 고정.
- **대표 채점표(07-14).** `representative_score = 객관성 0.6 + 근거성 0.4`(분모 16 라벨 보정). 근거성 초안은
  본문 풍부함 밀도(`body_richness`)였으나 **홍보 기사가 밀도 더 높게 나오는 함정**을 발견 → `sentence_coverage`
  (근거 든 문장 비율)로 방향 선회. spec·plan v2로 채점표·2단계 클러스터링·백필을 전면 상세화.
- **근거성 확정 + main 배선(07-15).** 근거성을 **독립 출처 커버리지**(`source_coverage`, 자기인용 제외)로
  교체. `curate.main`에 채점 기반 대표선정·길이필터(300~1500)·코어단어 주제/가중치·density 순위 배선.
- **2단계 클러스터링.** 단일 패스 → **매체 내부(중복 축소) → 매체 간(교차검증)** 2단계로 분리해 한 매체가 같은
  사건을 여러 건 쏟을 때 교차검증수가 부풀던 결함(C4) 차단.
- **코어단어 + 평판.** 그날 주제 top-3 자동 추출·가중치 누적(날짜 멱등), 선택률 평판(`win/appear`) 축적 배선.
  기자 부실 스트라이크 기록을 요약 단계→선별 대표후보 판정 시점으로 이동.

### 주요 결정과 그 이유 (고민한 지점)
- **근거성 = 밀도 아니라 독립 출처 커버리지.** 밀도(문장당 근거 수)는 보도자료가 오히려 높아 역효과. "몇 개의
  **독립** 출처가 뒷받침되나"가 진짜 근거성 → 자기인용 제외한 source_coverage로 확정.
- **제목 룰과 본문 룰 격리.** 기존 감점 룰에 제목 전용이 섞여 본문 채점을 오염 → `scope:title` 태그로 분리.
- **평판은 자동 산출(유지비 0).** 코어단어·주제·선택률 전부 데이터에서 파생, 사람이 손댈 파일 없음(G5).

### 산출물
- `objectivity.py`(body_objectivity·source_coverage·representative_score), `core_words.py`(추출·가중치·주제),
  `curate.py`(2단계 클러스터·main 배선), 선택률 평판(`update_selection_rates`), 라벨 픽스처, spec/plan v2.

### 이어서
- 수동 우선순위 완전 제거(G3)·매체 확장·통합 게이트(낚시/블랙리스트)·라이브 통합(Phase 6).

## 11일차 — 2026-07-16~17 (매체 확장 + 통합 게이트·선택률 서열 + 관찰 착수, D4~D8)

### 원래 일정
- 선별재설계 Phase 6(통합·라이브) 진입. 수동 우선순위 죽은 배선 정리 + 게이트·서열 확정 + 관찰 모드 개시.

### 실제 작업
- **매체 확장 + 우선순위 폐지(07-16).** `sources.yaml` 수동 우선순위 필드 폐지, 매체 33피드로 확장. 죽은 배선
  제거(G3 마무리). 한국경제 gzip 응답 파싱 실패를 **바이트 직접수신 재파싱**으로 구제(`fetch.py`).
- **D4·D5 통합 게이트(07-17).** 대표 선정 시점에 하드 게이트(제목 낚시·블랙리스트·길이) + 관찰 게이트(본문 점수)
  통합. 블랙리스트를 pool 선제거→대표 게이트로 이동(부실 기자 기사도 **사건 증거로는 유효** → 멤버로 남기고
  대표만 박탈). 코어단어 성격 없는 토큰 배제 + 가산 상한(오병합 차단).
- **D6·D7 선택률 서열.** 파이프라인 서열을 density(감점 밀도)→**선택률**(win/appear)로 교체. density는 연좌제
  (좋은 매체의 낚시 통과/나쁜 매체의 좋은 기사 밀림) 문제라 브리핑 관찰축으로만 강등. 동점 tie-break 최신순 추가.
- **D8 최소 3매체 + 백필 삭제.** 단독 백필(빈 슬롯을 낚시·홍보 물량으로 채움) 제거, 정렬을 매체 다양성↓→최신
  2키로 단순화, 코어단어 가중치는 관찰용 강등.
- **관찰 인프라.** `--dry-run` 모드(점검 반복이 `scores/` 누적 상태를 오염하던 사고 유형 차단), 40건 상한
  (A6, 10×4). decision-log §5에 관찰 대기 항목 기록(브리핑 연동), 개발기획서 §9 as-built(D2~D8) 동기화.

### 주요 결정과 그 이유 (고민한 지점)
- **연좌제(매체 단위) 대신 기사 단위 판정.** 낚시·홍보는 매체가 아니라 기사가 문제 → density 매체 연좌제를
  버리고 대표 게이트가 기사 단위로 거른다. 좋은 매체의 나쁜 기사, 나쁜 매체의 좋은 기사를 다 옳게 처리.
- **블랙 기자 기사도 클러스터 멤버로 남김.** 대표 자격만 박탈하고 교차검증엔 기여 — 사건 존재의 증거는 유효.
- **백필 삭제 = "억지로 안 채움".** 3매체 미달은 노출 안 함(하루 ~28~31건). 로드맵 §8 원칙과 일치.
- **하한값은 코드가 아니라 데이터로.** `REP_SCORE_FLOOR`를 하드 배제 대신 **관찰 로그**로 둬 분포를 먼저 본다.

### 산출물
- `sources.yaml`(33피드·우선순위 폐지), `fetch.py`(gzip 구제), `curate.py`(통합 게이트·3매체·dry-run·백필삭제),
  `objectivity.py`(선택률 서열), `limits.yaml`(40건), `기획/decision-log.md`(§5), 개발기획서 §9. 166 테스트 통과.

### 이어서
- **관찰 모드 운영.** 브랜치 미병합 상태로 며칠 라이브 로그(gate-excluded 분포)를 보고 하한값·병합 판단.

## 12일차 — 2026-07-18~20 (관찰 운영 + 로그 정밀화, 미병합 유지)

### 실제 작업
- **관찰 로그 fix(07-18).** dry-run에서도 `gate-excluded` 로그를 기록하도록 수정(관찰은 로컬 dry-run으로만
  쌓이므로 dry-run이 로그를 빠뜨리면 분포를 못 봄). 이메일에서 카카오톡용 plain 블록 제거(notify).
- **분포 관찰 확대(07-20).** 대표 점수 관찰을 **미달만 → 통과 후보 전원 로그**로 확대(미달만 기록하면 분포
  전체를 못 봐 하한값을 못 정함). 본문 추출 선택자 보강.
- **A5 첫 분포 확보.** 07-20 gate-excluded: min 0.45·median 0.68·max 0.92, `REP_SCORE_FLOOR`(0.35) 미만 0건
  → 0.35는 유명무실, 하한 후보는 0.45~0.6 구간임을 데이터로 확인. 제목 낚시 게이트는 과배제 없음 확인 후 확정.

### 주요 결정과 그 이유
- **관찰은 "전원 로그"라야 의미.** 미달만 남기면 분포를 못 봐 하한을 못 정함 → 통과 후보 점수를 전부 스냅샷.
- **브랜치 계속 미병합.** 라이브에 안 돌면 로그가 안 쌓이므로 매일 dry-run으로 관찰. 하한 확정 전까진 병합 보류.

### 산출물
- `curate.py`(dry-run 로그·전원 관찰·본문 선택자), `notify.py`(카톡 plain 제거), `scores/gate-excluded-*.json`.

### 이어서
- A5 하한 승격 판단(며칠 분포 더) → 확정되면 병합.

## 13일차 — 2026-07-21 (선별재설계 관찰 중 IT/테크 소스 정제, `feat/selection-redesign`)

### 원래 일정
- 정식 로드맵(~07-10 M4)은 종료. 현재는 `feat/selection-redesign`(D2~D8) 관찰→병합 단계.
- 오늘 목표: 브리핑 + dry-run으로 관찰 로그 축적, 관찰 항목 점검.

### 실제 작업 (브리핑 중 발견한 IT/테크 결함을 근본 수정)
- **브리핑 + dry-run.** 시스템 정상(다이제스트 커밋·실패 0), 한국경제 13일째 1위. dry-run으로
  `gate-excluded-2026-07-21.json` 축적 — A5 분포 2일차 n=113 min 0.45·median 0.67(07-20과 동일 패턴).
- **IT/테크 1건 이상 추적 → 근본 원인 규명.** dry-run에서 IT/테크 선택 1건뿐. 수집·클러스터는 정상이고
  선별 게이트가 원인. 진단: 분야=피드버킷(`fetch.py` `category`)이라 **주제 검증이 없음** → 전체기사
  (allArticle) 피드가 매체의 경제·사회 기사를 IT/테크 칸에 통째로 쏟음.
- **노이즈 실태 계측.** 디지털투데이 `소유 주식 수량 증가` 공시 봇 스팸(36/50), IT조선 제약 실적 등
  전체기사 피드 3개가 발원지 확인. MIN_MEDIA=2 완화 실험은 카뱅파업·홈플러스 같은 타분야 노이즈로만
  채워짐 → 게이트 완화는 증상 대응임을 데이터로 확인.
- **A안(소스 정제) 적용.** 세 매체의 AI/테크 섹션 피드를 실측 확인 후 교체: IT조선 `S1N11`(컴퓨팅·AI),
  디지털투데이 `S1N10`(AI·엔터프라이즈), 바이라인 `category/ai/feed/`. 교체 후 재수집 결과 거의 전부 AI.
- **IT/테크만 MIN_MEDIA=2.** 소스 정제로 노이즈를 발원지에서 잡은 뒤, 테크는 3곳 독립보도가 드문 도메인
  특성을 반영해 문턱만 완화(`MIN_MEDIA_BY_CAT`). dry-run: IT/테크 **0→8건 전부 테크**, 타 분야 무영향.
- **경제 확인.** 7/10 미달은 노이즈·버그 아님 — 교차≥3 클러스터가 7개뿐이고 7개 전부 선택(게이트 탈락 0).
  고볼륨 도메인이라 3매체가 진짜 중요도 신호 → **경제는 3 유지**로 결정.
- **커밋 + 테스트 + 문서.** `test_curate_min_media.py` 추가(전체 168 통과), decision-log §5에 A7 기록·A5
  분포 갱신. 커밋 a1e76cd.

### 주요 결정과 그 이유 (고민한 지점)
- **게이트 완화가 아니라 소스 정제가 근본.** IT/테크 1건은 증상. MIN_MEDIA를 낮추면 채워지지만 타분야
  노이즈로 채워짐(데이터로 확인). 발원지(전체기사 피드)를 섹션 피드로 바꿔야 노이즈가 사라짐. 분야=피드
  버킷이므로 "섹션이 곧 필터"라 코드 0으로 해결.
- **분야 쪼개기(IT/AI vs 테크)는 반려.** 사용자 제안이었으나 동기가 "노이즈"임을 확인 → 쪼개기는 노이즈를
  안 없애고 5번째 분야로 웹계약·이메일·limits·다이제스트까지 파급 + 풀을 더 얇게 만듦(YAGNI). 노이즈는
  섹션 피드로 이미 해결되므로 분리는 접음(볼륨 두터워지면 별건으로 재검토).
- **IT/테크만 2매체, 경제는 3 유지.** 두 분야의 3매체 의미가 반대. IT/테크는 3곳 독립보도가 구조적으로
  드물어(단독·전문 기사) 3은 비현실적 문턱. 경제는 고볼륨이라 3매체가 "많은 매체가 크게 다룬 사건"이라는
  중요도 신호 → 낮추면 바를 스스로 무너뜨림. 도메인별로 다르게 두는 게 맞음.
- **미달을 억지로 안 채움.** 경제 7건은 "오늘 크게 다뤄진 경제 뉴스가 7건"이라는 정직한 신호(로드맵 §8).

### 산출물
- `sources.yaml`(IT/테크 전체기사 피드 3개→AI섹션), `src/curate.py`(`MIN_MEDIA_BY_CAT`),
  `tests/test_curate_min_media.py`, `기획/decision-log.md`(§5 A7·A5). 커밋 a1e76cd(브랜치, 미병합).

### 이어서
- **관찰 계속(다음 브리핑).** A5 `REP_SCORE_FLOOR` 2일 연속 min≈0.45 → 하루 더 보고 0.45~0.5 승격 판단.
  A7 IT/테크 정제 → 라이브 며칠 볼륨·품질 관찰.
- 위 관찰 끝나면 `feat/selection-redesign`을 `main` 병합(cron 라이브 반영).

## 14일차 — 2026-07-22 (선별재설계 병합·라이브 + density 완전 제거)

### 원래 일정
- `feat/selection-redesign`(D2~D8) 관찰→병합 단계 마무리.
- 오늘 목표: 브리핑 + dry-run으로 A5 분포 3일차 확인 → 하한 승격·병합 판단.

### 실제 작업
- **브리핑 + dry-run.** 시스템 정상(다이제스트 커밋·실패 0), 한국경제 14일째 1위. A5 분포 3일차
  n=131 min 0.56·median 0.68, 0.35 미만 0건(3일 연속 min 0.45~0.56).
- **A5 하한 승격·확정.** `REP_SCORE_FLOOR` 0.35 관찰선 → **0.45 하드 하한**. 미달은 대표 자격 박탈
  (reason=`low_score`), 통과분은 계속 로그(`score_observed`)해 분포 추적 유지. dry-run 선택 10/10/10/5
  불변·low_score 1건(클러스터 타 멤버로 생존)·과배제 없음. 168 테스트 그린. 커밋 eb54a5c.
- **선별재설계 병합·push.** `feat/selection-redesign` → `main` 병합 → `origin/main` push(1658bed).
  선별재설계가 **프로덕션 cron에 처음 라이브**(내일 아침부터 새 선별 로직 적용).
- **선택률 데이터 진단(중요).** "지켜보자"던 선택률(`selection_rate`=win/appear)이 실은 **0건**이었음을
  발견. 원인: ①누적 코드가 브랜치에만 있어 cron 미반영 ②dry-run은 `scores/` 미기록. 그래서 브리핑에서
  "선택률 서열"로 여긴 rank-history는 실은 **density(감점밀도) 기반 구 파이프라인** 순위였음(density
  오름차순과 정확히 일치 확인). push해야 비로소 누적 시작 = 닭-달걀이라 push가 관찰의 전제였다.
- **density 완전 제거 → 선택률 일원화.** density가 이미 선별 미사용(관찰 오버헤드)이라, 매체 지표를
  선택률로 일원화하기로 하고 density 개념을 코드에서 통째 삭제. 브레인스토밍→스펙→계획→인라인 4태스크:
  - 삭제: `objectivity.update_media_scores`(매체 density/attr/outlier 누적)·`compute_ranks`(density 순위)·
    당일 `density_per_1000`. `media.json`·rank-history **리셋**(`{media,processed_dates,selection_dates}`).
  - 교체: rank-history를 `compute_selection_ranks`(선택률)로. 필터를 `article_count`→`appear_total>0`
    (density 필드 제거 대비 — 놓치면 순위 전원 탈락 버그). `process_date`는 당일 리포트만 저장하고
    `processed_dates` 멱등 자체 관리(선택률 멱등은 독립키 `selection_dates`, curate 흐름).
  - 유지: penalties 채점기(`score_article`/`title_penalty`/`representative_score`)·당일 감점 메모. density 아님.
  - 검증: `grep -rn density src/` **0건**, 168 테스트 그린, curate dry-run 선별 불변. `main` 병합·push(9503050).
- **경제 연합뉴스 쏠림 분석.** 발행분 경제 10/10 연합뉴스 → 구 로직 `rep=min(members, key=우선순위)`가
  원인. 연합(통신사) 물량이 압도적이라 거의 모든 클러스터의 멤버 + 순번 1위 한국경제가 그 사건들엔 없어
  연합(2위)이 자동 대표. 새 로직은 `대표=본문 객관성 점수→선택률→최신순`이라 dry-run 경제가 매일경제·
  한국경제·연합·매일신문 4개 매체로 분산(연합 5/10) — 통신사 물량이 아니라 본문 품질로 선정.

### 주요 결정과 그 이유
- **push가 관찰의 전제.** 선택률은 cron 라이브 누적으로만 쌓임(dry-run 미기록) → 관찰하려면 반드시
  병합·push 선행. "며칠 dry-run으로 관찰"은 REP_SCORE_FLOOR엔 통했지만 선택률엔 안 통함.
- **density는 폐기, 되살리지 않음.** 선별에 이미 기여 0인 순수 관찰 오버헤드. 선택률로 신호 일원화(브리핑
  지표 하나 = 혼란↓, 유지보수↓). 당일 감점 규모는 `total_points`가 지키므로 density_per_1000도 함께 제거.
- **density ≠ 감점 채점기.** penalties.yaml 채점(`score_article`)은 낚시 게이트·본문점수·당일 감점 메모에
  쓰이므로 불변. 제거 대상은 "매체별 누적분"뿐임을 명확히 구분.

### 산출물
- `src/objectivity.py`(update_media_scores·compute_ranks 삭제, 필터·process_date 재배선, main 선택률 출력),
  `src/curate.py`(docstring), `tests/`(density 테스트 삭제·선택률로 갱신), `scores/media.json`·
  `media-rank-history.json` 리셋, `.claude/skills/daily-briefing/SKILL.md`(선택률화), `기획/decision-log.md`(§5),
  스펙·계획 문서(`docs/superpowers/{specs,plans}/2026-07-22-density-제거*`). 커밋 1658bed·9503050(main, push 완료).

### 이어서
- **선택률 누적 관찰(내일부터).** cron이 매일 `selection_rate` 누적. 초기 며칠은 전원 0/0이라 순위가
  매체명순(정상). 며칠 뒤 `scores/media.json`의 `selection_rate`(win/appear)로 서열 신뢰도 검증.
- **다음 결정:** 선택률을 선별에 더 강하게 반영할지(감점/클러스터 순위) — 데이터 축적 후 판단.
