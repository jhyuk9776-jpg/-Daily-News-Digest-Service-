# 결정 로그

이 문서는 앞으로 프로젝트를 진행하며 확정한 내용을 기록한다.  
아직 정하지 않은 것은 `미정`으로 둔다.

## 1. 핵심 결정 현황

| 항목 | 현재 결정 | 상태 | 메모 |
|---|---|---|---|
| 프로젝트 목표 | 매일 아침 사실 중심 뉴스 다이제스트 생성 | 확정 | Phase 1 기준 |
| 뉴스 범위 | 국내 + 해외 포함 | 확정 | 경제, 세계, IT 흐름 보강 |
| 1차 결과물 | 날짜별 마크다운 파일 | 확정 | 웹/앱은 이후 |
| 구현 방식 | 로컬 Python 스크립트 | 임시 확정 | 자동화 방식은 추가 결정 필요 |
| 뉴스 분야 | 경제, 사회, 세계, IT/테크 | 확정 | 정치 제외 |
| 정치 포함 여부 | Phase 1 제외 | 확정 | 추후 옵션 처리 |
| 뉴스 출처 | `sources.yaml` 목록으로 확정 | 확정 | 2026-07-01. 상세는 1.3 |
| 출처 우선순위 | 객관적 서술 최우선(피드백 기반 개선) | 확정 | 2026-07-01. 상세는 1.3 |
| 하루 기사 수 | 분야별 2건, 총 8건 | 확정 | 읽는 시간 5분 기준 |
| AI 모델 | OpenAI 저비용 모델 우선 검토 | 확정 | Claude는 품질 비교 후보 |
| 자동 실행 방식 | 수동 실행 후 GitHub Actions 우선 검토 | 확정 | 품질 확인 뒤 자동화 |
| 저장 위치 | `News/YYYY-MM-DD.md` | 확정 | 기존 폴더와 연결 가능 |
| 첫 달 작업 비용 | 22달러 | 확정 | Claude Code 월 구독 기준 |
| 웹/앱 언어·스택 | TypeScript (Next.js 웹 + Expo/React Native 앱) | 확정 | Phase 3 적용. 백엔드 Python 유지 |
| 서비스 성격(현 사이클) | 개인용(비상업) 우선 — 무료 공개·법무 라이트 체크는 유료화 시로 연기 | 확정 | 2026-07-06. RSS 개인 이용 범위로 매체 확장 가능(연합·한겨레 추가) |

## 2. 다음에 바로 정할 질문

1. 분야별로 어떤 RSS를 2~3개씩 사용할 것인가?
2. 중복 사건이 있을 때 어떤 출처를 우선할 것인가?
3. SBS처럼 개인/비상업 사용 조건이 있는 RSS를 Phase 1에서 어디까지 사용할 것인가?
4. 첫 달 이후 비용 상한을 얼마로 둘 것인가?

## 1.1 증거 기반 품질 전략 (2026-06-30 결정)

장기 비전(AI 편집장 플랫폼)을 위해 "증거 우선" 방향을 채택한다. 단, 수집 단계에서
하드 필터링하지 않는다. 데이터를 넓게 수집하고 선별 단계에서 순위 신호로 쓴다.

| 항목 | 결정 | 시점 |
|---|---|---|
| 수집 단계 필터 | 하지 않음. 넓게 수집(데이터 손실·분야 미달 방지) | Day 2 (현행 유지) |
| 증거 점수 랭킹 | 휴리스틱(숫자·%·기관명·실명 인용·날짜)으로 선별 시 가점. AI 미사용 | Day 3 |
| 교차 검증 신뢰도 | 2개 이상 독립 매체가 같은 사실 보도 시 신뢰 가점 + 중복제거 활용 | Day 3 |
| 사실 단위 출처 표기 | 다이제스트의 각 사실에 출처 링크 1:1 부착. "출처 없는 사실은 싣지 않는다" | Day 4 |

근거: 증거는 통계만이 아니라 판결·정책 발표·공식 발언도 포함한다. 하드 필터는
중요 뉴스를 버리고 분야별 2건을 못 채울 위험이 있어 랭킹 방식으로 간다.

## 1.2 Day 4 요약 단계 결정 (2026-06-30 확정)

| 항목 | 결정 | 메모 |
|---|---|---|
| AI 모델 | Claude Haiku 4.5 | 저비용·고정형식 요약에 적합. 하루 8건이라 월 ~$1 |
| API 경로 | **Replicate 경유** (`anthropic/claude-4.5-haiku`) | Anthropic 콘솔 결제가 막혀(카드/검증) Replicate 공식 채널로 같은 모델 사용. SDK는 `replicate` |
| API 키 관리 | `.env` + `.gitignore` | 키는 절대 커밋 안 함. 환경변수 `REPLICATE_API_TOKEN` |
| 제약 메모 | `max_tokens` 최소 1024 / 잔액 $5 미만 시 분당 6건 제한 | 429 시 자동 대기·재시도로 처리. $5+ 충전 시 제한 해제 |
| 본문 추출 | 빈/짧은 요약만 보강(BeautifulSoup) | 대상은 최종 8건뿐이라 비용 거의 0 |
| 본문 추출 폴백 | 같은 사건(클러스터) 내 다음 우선순위 매체로 순차 재시도, 전부 실패 시 "추출 실패로 제외" 안내 | related_links를 우선순위 순으로 시도 |
| 요약 호출 단위 | 기사별 개별 호출 | 격리성·출처 1:1 정확성·URL 캐시. Haiku라 호출수 비용 무시 가능 |
| 출처 표기 | 사실 1건당 출처 링크 부착, "출처 없는 사실은 싣지 않는다" | 1.1과 연동 |

## 1.3 출처·우선순위·자동화 결정 (2026-07-01 확정)

Day 5 시작 시 아래를 확정했다.

### 뉴스 출처 (확정)

`sources.yaml`에 등록된 목록을 정식 확정한다. 이용 조건이 불명확하거나 출처가
불분명한 매체는 제외한다.

| 분야 | 출처 (우선순위 순) |
|---|---|
| 경제 | 한국경제 → 매일경제 → 매일신문 → SBS |
| 사회 | 경향신문 → 매일신문 → SBS |
| 세계 | 경향신문 → 매일신문 → SBS |
| IT/테크 | 전자신문 → ZDNet Korea |

- `이코노미스트 타임스`(economist-times.com)는 출처 불분명으로 **제외**(2026-07-01).

### 출처 우선순위 = 객관적 서술 최우선 (확정)

| 항목 | 결정 |
|---|---|
| 최우선 기준 | 사실·수치·기관·인용 중심의 **객관적 서술**을 최우선 순위로 둔다 |
| 개선 방식 | AI가 고른 기사 중 사용자가 본문을 읽고 "객관적"이라 판단한 것을 피드백으로 축적, 반복 튜닝 |
| 현재 구현 | `curate.py`의 `evidence_score`(숫자·%·기관명·인용·기간표현 가점)가 1차 근사 |
| 향후 추가 | AI_CONTEXT §6의 평가어("논란이 커지고 있다" 등) **감점** 신호 추가 검토 |
| 객관성 예시 1호 | https://n.news.naver.com/mnews/article/088/0001016790 (사용자 지정 좋은 예시) |
| 감점 예시 1호 | 2026-07-01 IT/테크 [JB금융 ESG 보고서 기사](https://zdnet.co.kr/view/?no=20260701144713) — 제목 "성과 입증"은 홍보성 평가어이고 분야 부적합(은행 ESG↔IT/테크). 불릿 수치는 객관적이나 숫자가 있다는 이유로 선별을 통과. Phase 2 "매체 객관성 동적 점수"(일정-로드맵 §9)의 감점 튜닝 케이스로 축적 |

### 요약 견고성 보강 (확정, Day 5 구현)

자동화 전에 무인 실행에서 조용히 품질이 새는 것을 막기 위해 요약 단계를 보강했다.
근거: 짧은 RSS(102자)가 길이 기준을 통과해 부실한 채로 요약되어 모델이 대화체로
응답 → 불릿 0개 → 폴백 없이 실패하는 비결정적 버그를 발견(2026-07-01 매일경제 기사).

| 항목 | 결정 |
|---|---|
| 프롬프트 강화 | "오직 `- ` 불릿만 출력, 인사·설명·되묻기·본문요청 금지, 낼 사실 없으면 아무것도 출력 안 함" 명시 |
| 불릿 0개 폴백 | 짧은 RSS → 전체 본문 추출로 재요약. 그래도 0개면 같은 사건의 다음 우선순위 매체 본문으로 순차 재시도(마지막 순위까지) |
| 최후 처리 | 모든 후보에서 0개면 사실 요약 생략, 1순위 링크만 남김 |
| 단독 기사 | 교차보도가 없어 폴백 대상이 없으면 본문 추출+프롬프트 강화가 주 방어선 |
| 검증 | `tests/test_fallback.py`(unittest 6건) + 실제 파이프라인 재실행으로 요약실패 0 확인 |
| 구현 위치 | `extract.iter_contents`(후보 제너레이터), `summarize.summarize_item`(폴백 오케스트레이션) |

### 선별 정렬 기준 변경 (확정, Day 5)

증거점수가 최대 5점에 몰려 변별력이 약하고 감정적 제목도 숫자만 있으면 고득점하는
한계가 확인되어, 선별 정렬에서 **증거점수를 제외**한다(값은 기록용 메타데이터로 유지).

| 순위 | 기준 | 의미 |
|---|---|---|
| 1순위 | 교차검증수 ↓ | 여러 독립 매체가 보도한 사건일수록 위로 |
| 2순위 | 매체 우선순위 ↑ | sources.yaml 순위가 높은 매체 우선 |
| 3순위 | 발행 최신순 | 앞 두 기준 동점 시 임의성 제거(가장 최근 뉴스) |

- 증거점수(1.1의 "증거 점수 랭킹")는 **더 이상 랭킹에 쓰지 않는다.** 객관성 반영은
  Phase 2 "매체 객관성 동적 점수"(일정-로드맵 §9)로 데이터를 모아 제대로 만든다.
- 구현: `curate.recency_key` 추가, `select()` 정렬 키 수정.

### 자동화 방식 (확정)

| 항목 | 결정 |
|---|---|
| 실행 | GitHub Actions cron (매일 아침 자동 실행) |
| API 키 | GitHub Secrets(`REPLICATE_API_TOKEN`)로 관리, 코드 미포함 |
| 결과 저장 | 생성된 `News/YYYY-MM-DD.md`를 **main 브랜치에 자동 커밋** |
| 로그 삭제 | 넣지 않음. 데이터가 가볍고 Phase 2/3 웹·앱이 과거분을 읽어야 하므로 보존 |

## 1.4 객관성 점수 자동 채점 (2026-07-03 확정)

감점 사전(`penalties.yaml`)이 코드가 읽는 기준으로 배선되고 등급 가중 채점이 붙으면서,
"수동 채점을 놓친 날은 raw가 소멸해 영영 채점 못 함"(예: 07-02)을 구조적으로 없앤다.

| 항목 | 결정 | 근거 |
|---|---|---|
| 채점 실행 | **cron이 매일 요약 직후 자동 실행**(`daily.yml`에 `objectivity.py` 스텝) | 수동 누락 방지. fetch가 만든 raw로 채점해 디제스트와 동일 스냅샷 |
| 비용 | 0 | fetch·채점 모두 AI 토큰 미사용(요약만 Replicate) |
| raw 보관 | **안 함**(runner에서 소멸) | 하루 ~1000건×매일 → 저장 비대. `scores/`(누적)만 커밋 |
| 재채점(backfill) | **미지원**(설계상) | raw가 없어 과거 소급 불가. 사전 크게 바꾸면 `media.json` 리셋해 새로 누적 |
| 모드 | observe-only 유지 | 랭킹·제외 미반영. `curate/summarize/run.sh` 불변, CI 스텝으로만 추가 |
| 브리핑 | 커밋된 `scores/`를 **읽기만** | 로컬 재채점 안 함. `objectivity-score` 스킬은 수동 재실행·리셋·검수용 |

- 감점 기준은 `penalties.yaml`에서 편집(코드 수정 아님). 등급 weight(strong15/medium8/weak3),
  `observe_candidates`(감점 0·기록만), `exclusions`(단독 모호어 금지) 구조. 상세는 로드맵 §6.

## 3. 확정값과 남은 추천값

아래는 빠르게 MVP를 만들기 위해 확정한 값과 아직 남은 추천값이다.

| 항목 | 추천값 |
|---|---|
| 분야 | 경제, 사회, 세계, IT/테크 |
| 정치 | 초기에는 제외하거나 별도 옵션 처리 |
| 기사 수 | 분야별 2건, 총 8건 |
| 출처 | RSS를 제공하는 국내 주요 매체 + 해외 공신력 매체 |
| 결과물 | `News/YYYY-MM-DD.md` |
| 자동화 | 처음은 수동 실행, 이후 GitHub Actions |
| 비용 | 첫 달 22달러, Claude Code 월 구독 기준 |

## 3.1 RSS 출처 결정 기준

RSS 출처는 아직 확정하지 않았다. 먼저 RSS가 안정적으로 제공되는지, 이용 조건이 개인/비상업 사용을 허용하는지, 분야별 중복을 얼마나 줄일 수 있는지 확인한다.

| 항목 | 결정 기준 |
|---|---|
| 시작 개수 | 전체 8~12개 RSS |
| 분야별 개수 | 경제, 사회, 세계, IT/테크별 2~3개 |
| 우선순위 | 공신력, 원문성, RSS 안정성, 발행 시간 정확도 |
| 사용 방식 | 제목, 요약, 발행 시간, 원문 링크 중심 |
| 제외 기준 | RSS 불안정, 이용 조건 불명확, 광고/연예/스포츠 중심, 중복 과다 |
| 상업화 시 | 언론사 제휴/API/이용약관 재검토 |

조사 메모:

- BuiltWith 기준 RSS 사용 라이브 웹사이트는 38,136,800개, 한국 라이브 사이트는 83,842개로 집계되어 있다.
- SBS는 RSS 서비스를 제공하며, 개인 이용자의 비상업적 사용만 허가한다고 안내한다.
- 언론사별 RSS 구독 순위나 "가장 많이 쓰이는 RSS"에 대한 신뢰 가능한 공개 최신 순위는 찾지 못했다.
- 따라서 Phase 1은 인기 순위보다 공신력, 안정성, 분야 적합성을 기준으로 RSS를 고른다.

## 3.2 웹/앱 언어·스택 결정 (2026-06-29 확정)

웹과 앱을 함께 운영하기 위한 언어·스택을 **TypeScript(React 계열)**로 확정한다.

| 항목 | 결정 |
|---|---|
| 언어 | TypeScript |
| 웹 | Next.js (PWA 포함) |
| 앱 | Expo / React Native |
| 백엔드 | 기존 Python 파이프라인 유지 |
| DB 연동 | Supabase (Phase 2 도입) |

결정 이유:

- 언어 하나로 웹+iOS+안드로이드를 모두 커버해 솔로 개발 부담이 가장 작다.
- Supabase 공식 지원(JS SDK)이 가장 두터워 Phase 2 DB 결정과 직결된다.
- 생태계·자료·AI 코딩 도구 지원이 넓어 혼자서도 빠르게 진행 가능하다.
- 뉴스 다이제스트는 텍스트·리스트 중심 읽기 앱이라 웹 SEO/공유에 강한 스택이 유리하다.

실행 방식:

1. **Next.js로 PWA 웹을 먼저 출시**해 사용성을 검증한다.
2. 검증 후 같은 TypeScript 자산으로 **Expo 네이티브 앱**을 확장한다.
3. 이 작업은 **Phase 3 이후**다. 지금은 Python 데이터 파이프라인 완성이 우선이며, 이 결정은 기록만 해둔다.

## 4. 오늘의 방향

2026-06-29 기준 오늘은 구현을 바로 시작하기보다, Phase 1의 범위를 더 선명하게 고정한다.

- 큰 비전: AI 뉴스 편집장 플랫폼
- 현재 범위: 사실 중심 뉴스 다이제스트 생성기
- 오늘 목표: 분야, 출처, 기사 수, 비용, 저장 위치, 자동화 방향 확정
- 비용 원칙: 무료 RSS와 로컬 실행을 우선하고, 비용은 AI 요약 API에만 집중한다.
- 구현 원칙: 하루 8건으로 작게 시작해 결과물 품질을 먼저 검증한다.

## 5. 선별재설계 관찰 대기 항목 (2026-07-17, 미정)

`feat/selection-redesign` 브랜치가 D2~D8 구현을 끝냈으나, 하한값을 데이터로 정하려고
**관찰 모드**로 둔 항목들. 며칠 라이브 로그를 보고 확정한다. **브리핑에서 매일 확인할 것.**

| 항목 | 현재 상태 | 확정 조건 |
|---|---|---|
| A5 대표 본문 점수 하한(`REP_SCORE_FLOOR`=0.35) | 관찰 모드 — 미달을 `scores/gate-excluded-<date>.json`에 로그만, 배제 안 함 | 며칠 점수 분포 확인 후 하드 하한으로 승격(홍보는 하한 밑, 진짜 기사는 위인지) |
| A5 제목 낚시 게이트 | ON(하드 배제) — 과배제 여부 관찰 | gate-excluded의 `clickbait` 사유가 진짜 기사를 떨구지 않는지 |
| A6 40건 상한 | 적용됨(경제·나머지 10). 라이브 볼륨 관찰 | 며칠 품질 확인 |
| 브랜치 병합 | **미병합** — cron 라이브엔 아직 반영 안 됨 | 위 관찰이 끝나면 `main` 병합 |

> ⚠️ **관찰의 전제:** 이 브랜치가 라이브에 안 돌면 `gate-excluded` 로그가 안 쌓인다. 관찰하려면
> 병합하거나 로컬에서 매일 `python src/curate.py --dry-run`을 돌려 로그를 확인해야 한다.
> 브리핑 시 `git ls-tree`로 브랜치 병합 여부와 `scores/gate-excluded-*` 존재를 함께 점검.
