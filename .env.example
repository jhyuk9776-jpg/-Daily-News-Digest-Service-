# Replicate API 토큰. replicate.com → Account → API tokens 에서 발급.
# Claude Haiku 4.5를 Replicate 경유로 호출한다(모델: anthropic/claude-4.5-haiku).
# 실제 토큰은 .env 파일에 넣으세요(.env는 .gitignore로 커밋되지 않음).
REPLICATE_API_TOKEN=r8_여기에-토큰-붙여넣기
