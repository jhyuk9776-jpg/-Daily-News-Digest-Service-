# 개발 기획서 — 선별 재설계 (Selection Redesign, Phase 1~6)

작성일: 2026-07-14
작성: 이재혁
대상 독자: 구현 엔지니어 (이 문서만 읽고 만들 수 있게 작성)
기준 스펙: `docs/superpowers/specs/2026-07-13-선별재설계-design.md` 최종 v2 · 메모리 `objectivity-tuning-next`
브랜치: `feat/selection-redesign` · 상태: **Phase 0~5 완료, Phase 6(통합·라이브)만 남음** (2026-07-16 기준, 156 테스트 통과)

> 이 문서는 포트폴리오용 서사가 아니라 **구현 근거 문서**다. 왜 이렇게 바꾸는지(원인)와 무엇을 어떻게 만드는지(설계·스키마)를 엔지니어가 바로 코드로 옮길 수 있는 수준으로 적는다.

---

## 1. 배경 · 원인

### 1.1 지금 어떻게 도는가 (Before)

현재 선별은 [`src/curate.py`](../../../src/curate.py)의 4단계다.

| 단계 | 함수 | 하는 일 |
|---|---|---|
| ① 날짜 필터 | `in_date_window` | 오늘·어제(KST) 기사만 통과 |
| ② 증거 점수 | `evidence_signals` | 숫자·%·기관·인용·기간 신호 0~5개 카운트 (메타데이터, 선별에 직접 안 씀) |
| ③ 클러스터링 + 교차검증 | `cluster_articles` | 제목 유사도 0.6으로 같은 사건 묶고, 대표는 **매체 우선순위 최상위**로 결정 |
| ④ 분야별 상위 N | `select` | 정렬키 `(-교차검증수, 매체우선순위, 최신순)` 후 분야별 `limit`건 |

### 1.2 무엇이 문제인가 (원인)

| # | 원인 | 근거 |
|---|---|---|
| C1 | **대표 선정이 본문 품질과 무관** | `cluster_articles`의 대표는 `priority_map`의 최소값(=수동 우선순위)으로만 뽑힘. 제목만 보고, 본문의 객관성·근거는 안 봄 |
| C2 | **매체 우선순위가 수동** | `sources.yaml`의 `우선순위` 필드를 사람이 관리 → 주관·유지비. 이미 폐지 방향으로 합의됨 |
| C3 | **교차검증수 편향** | 정렬 1순위가 `교차검증수`뿐 → 통신사 물량·받아쓰기 기사가 위로 올라감(물량 = 다양성 아님) |
| C4 | **같은 매체 중복 미처리** | 단일 패스 클러스터링이라 한 매체가 같은 사건을 여러 건 쏟으면 교차검증수가 부풀 수 있음 |
| C5 | **평판 데이터 없음** | 어떤 매체가 반복적으로 대표로 뽑히는지(신뢰도) 축적이 없어 데이터 기반 판단 근거가 없음 |

> 핵심: 지금은 "누가 먼저·많이 썼나"로 대표를 고른다. 바꾼 뒤에는 "누가 **객관적·근거 있게** 썼나"로 고르고, 그 결과를 **데이터로 축적**해 매체 평판을 만든다.

---

## 2. 목표

| 목표 | 성공의 모습 |
|---|---|
| G1 대표를 **본문 채점**으로 선정 | 클러스터 대표 = `객관성 0.6 + 근거성 0.4` 최고점 기사 |
| G2 매체 평판을 **데이터로 축적** | `media.json`에 매체별 `선택률 = win/appear` 누적 |
| G3 수동 우선순위 **완전 제거** | `sources.yaml 우선순위` 미사용. 매체 서열은 `compute_ranks` density 런타임 파생으로 대체 |
| G4 그날의 **중요 주제 반영** | 코어단어 자동 추출 → 주제 top-3 → 가중치 누적으로 정렬에 반영 |
| G5 **유지비 0** | 코어단어·주제·평판 모두 자동 산출. 사람이 손댈 파일 없음 |

**비목표(Non-goals):** LLM 기반 채점 도입(휴리스틱 유지), 카테고리 체계 개편, 웹/이메일 렌더링 변경. 이번 범위는 `curate` 파이프라인과 `scores/` 데이터뿐이다.

---

## 3. 아키텍처

### 3.1 부품 경계

각 부품은 순수 함수로 독립 테스트 가능해야 한다(입력 dict → 출력 dict, I/O는 경계에서만).

```
raw/<date>.json
   │ (전처리: 날짜 필터 + 블랙리스트 — 기존 유지)
   ▼
┌───────────────────────────────────────────────────────────┐
│ ① cluster_two_stage   같은 매체 내부 → 매체 간 2단계 클러스터 │
│ ② core_words          코어단어 집계 · 주제 top-3 · 가중치 갱신 │
│ ③ length_filter       본문 300~1500자 (⑥ 시점 지연 적용)      │
│ ④ rank_clusters       정렬(등장매체수 · 코어단어 · 가중치합)   │
│ ⑤ backfill            상위10 + density 라운드로빈 백필         │
│ ⑥ score_representative 대표 채점(객관성 0.6 + 근거성 0.4)      │
│ ⑦ update_reputation   appear/win 갱신 · 선택률 산출           │
└───────────────────────────────────────────────────────────┘
   ▼
selected/<date>.json  +  scores/{core-word-weights,topics-<date>,media}.json
```

> 🖼️ [이미지 자리] 파이프라인 Before(4단계) → After(7단계) 다이어그램 — 사용자 추가 예정

### 3.2 재사용할 기존 자산 (새로 만들지 말 것)

| 필요 기능 | 기존 자산 | 비고 |
|---|---|---|
| 매체 서열(density) | `compute_ranks` / `scores/media.json` | G3의 우선순위 대체. 재구현 금지 |
| 본문 객관성 감점 | `objectivity.body_objectivity(body, title)` | Phase 0 완료. `scope:title` 격리 적용됨 |
| 본문 근거성 | `objectivity.source_coverage` | Phase 0 완료. 원안의 `body_richness`(근거 밀도)를 대체 — 밀도는 홍보 기사가 더 높게 나오는 함정(§5.6) |
| 증거 신호 카운트 | `curate.evidence_signals(text)` | 문장 커버리지(근거성) 계산의 기본 단위 |
| 제목 정규화 | `curate.normalize_title` | 클러스터링에 재사용 |

---

## 4. 진행순서 변화 (Before 4단계 → After 7단계)

| | Before (4) | After (7) |
|---|---|---|
| 전처리 | 날짜 필터, 블랙리스트 | **동일 유지** |
| 1 | (증거점수 메타) | **2단계 클러스터링** (매체 내부 → 매체 간) |
| 2 | 클러스터링(대표=우선순위) | **코어단어 집계 · 주제 top-3 · 가중치 갱신** |
| 3 | 분야별 상위 N | **길이 필터** 300~1500 (지연 적용) |
| 4 | — | **정렬** (등장매체수↓ → 제목 코어단어 포함 → 가중치합↓) |
| 5 | — | **백필** (상위10 + density 라운드로빈) |
| 6 | — | **대표 채점표** (객관성 0.6 + 근거성 0.4, 동점=density) |
| 7 | — | **요약 + 평판 갱신** (appear/win → 선택률) |

**바뀌는 핵심 3가지:**
1. 대표 결정 기준: `매체 우선순위` → `본문 채점(객관성+근거성)`
2. 정렬 기준: `교차검증수 단일` → `등장매체수 + 코어단어 + 주제 가중치`
3. 결과 활용: `1회성` → `매체 평판 데이터 누적(선택률)`

---

## 5. 상세 설계

### 5.1 ① 2단계 클러스터링

**왜 2단계인가:** C4(같은 매체 중복). 한 매체가 같은 사건을 여러 건 내면 매체 간 교차검증수를 부풀린다. 먼저 매체 내부에서 접고, 그다음 매체 간에 묶는다.

- **1단계 (매체 내부):** 같은 `source` 안에서 제목 유사도 ≥ `SIMILARITY_THRESHOLD`(0.6) 이면 **최근 1건만** 남기고 나머지 축소.
- **2단계 (매체 간):** 남은 대표 후보들을 유사도 **+ 코어단어 공유**로 묶는다. 제목 유사도만으로 부족한 케이스를 코어단어 교집합(≥1개 공유)이 보강한다.

**클러스터 산출물(부품 출력):**
```
{
  "members":  [기사...],        # 매체 간에 묶인 전 멤버
  "sources":  {"연합뉴스", ...}, # 등장 매체 집합
  "media_count": 5,             # = len(sources)  (교차검증수 대체 명칭)
  "core_words": ["관세","상호관세"]
}
```

> 코어단어 공유는 유사도의 보조 신호다. 유사도 단독 오탐(다른 사건인데 제목만 비슷)·미탐(같은 사건인데 표현 다름)을 코어단어 교집합이 완충한다.

### 5.2 ② 코어단어 집계 · 주제 top-3 · 가중치 갱신

**코어단어 추출:** 제목에서 자동 추출(불용어 제거 후 명사 위주). **유지비 0**이 핵심 — 사전 수동 관리 없음.

**주제 점수:**
```
topic_score = 등장매체수 + 총기사수 × 0.2
```
- 매체수를 주가중으로 둔 이유: **다양성이 물량보다 객관적** 신호(통신사 1곳이 10건 쏜 것 < 서로 다른 5개 매체가 1건씩).
- 상위 3개 주제를 그날의 top-3로 확정 → `topics-<date>.json` 저장.

**가중치 갱신 (누적, `core-word-weights.json`):**
- 순위 가점: 1위 `5`, 2위 `3`, 3위 `1`
- 학습률 `α = 0.1`
- 갱신식: `weight[word] += rank_score × α` (top-3 주제에 속한 코어단어 대상)
- **빈 저장소 첫날:** top-3의 가점값이 그대로 초기값이 된다.

> ⚠️ 결정 필요: 감쇠(decay) 없이 가산만 하면 장기적으로 가중치가 단조 증가한다. 초기 운영에서는 가산만으로 두고, 값이 포화하면 EMA(`weight = weight×(1-α) + rank_score×α`) 또는 일정 주기 재정규화를 도입한다. **Phase 2 구현 시 실측 후 결정.**

### 5.3 ③ 길이 필터 (300~1500자)

**규칙:** 본문 글자수 `300 ≤ len ≤ 1500` 밖이면 대표 후보에서 이탈.

**지연 적용 (중요):** 논리적으로 3단계지만 **실행은 대표 채점(⑥) 직전**이다. 이유 = 전 후보의 본문을 미리 추출하면 비용이 폭증한다(C-비용). 따라서 정렬·백필(④⑤)로 상위 후보를 좁힌 뒤, **살아남은 클러스터의 대표 후보 본문만 추출**하며 길이 필터를 건다.

- 이탈 시 **로그 남김**(침묵 금지): 어떤 기사가 왜(너무 짧음/너무 김) 탈락했는지 기록.
- 길이 필터로 대표 후보가 전부 탈락하면 → 그 클러스터의 차선 멤버로 재시도, 그래도 없으면 클러스터 드롭(로그).

### 5.4 ④ 정렬

클러스터 정렬키(위로 올라올수록 우선):

| 순위 | 키 | 방향 | 의미 |
|---|---|---|---|
| 1 | `media_count` | 내림차순 | 여러 매체가 다룬 사건 우선(다양성) |
| 2 | 제목의 코어단어 포함 | 포함 우선 | 대표 제목이 그날 주제어를 담고 있으면 가점 |
| 3 | 코어단어 **가중치합** | 내림차순 | `sum(weight[w] for w in cluster.core_words)` — 누적 주제 중요도 |

### 5.5 ⑤ 백필 (다양성 보정)

- **분야별 상한**(`limits.yaml`, 기본 2·경제 10)만큼 1차 선정 — 교차검증된 클러스터(등장매체 2곳 이상) 우선.
  (원안은 전역 상위10이었으나 분야별 상한 유지로 확정 — 부록 A3.)
- 부족분은 **density 순위 기반 라운드로빈**으로 백필한다(단독 클러스터를 density 상위 3개 매체에서 돌아가며 채워 특정 매체 쏠림 방지).
- 목적: 상위권이 소수 매체로 채워지는 것을 막고 매체 다양성을 확보.

> 🖼️ [이미지 자리] 백필 라운드로빈 예시 표(상위10 + density 순환) — 사용자 추가 예정

### 5.6 ⑥ 대표 채점표

살아남은 클러스터별로, 멤버 중 대표를 **채점 최고점**으로 선정.

```
score = 0.6 × 객관성 + 0.4 × 근거성
```

| 성분 | 계산 | 근거 |
|---|---|---|
| 객관성 (0~1) | `objectivity.body_objectivity(body,title)`의 감점을 `1 - min(감점, 16)/16`로 역정규화. 감점↑ → 점수↓ | 서비스 목적이 객관성 → 주가중 0.6 |
| 근거성 (0~1) | **독립 출처 커버리지** = (독립 출처를 명시한 문장 수) / (전체 문장 수). `objectivity.source_coverage` | 홍보 기사의 자기인용을 근거로 안 침 |
| 동점 처리 | `media.json` density 높은 매체 우선 | 데이터 파생 서열로 tie-break |

**왜 근거성 = 독립 출처 커버리지인가 (구현 중 변경, 2026-07-14):** 원안은 "증거 신호(숫자·%·기관) ≥1개인
문장 비율"인 **문장 커버리지**였다. 총량 대신 비율을 쓴 이유는 유효했지만(감점1호 실측: 홍보인데 근거밀도
3.59 = 길이로 이기는 **함정**), 구현하며 **비율로도 홍보를 못 거른다**는 걸 발견했다. 보도자료는 자사 수치를
문장마다 뿌려서 커버리지까지 높게 나온다. 그래서 "숫자가 있나"가 아니라 **"누가 밝혔나"**로 기준을 옮겼다:
독립기관(통계청 등) 인용은 인정, 귀속표지(`~에 따르면`)는 자기지칭어가 없을 때만 인정. 이러면 자기인용으로
도배한 홍보 기사의 근거성이 0으로 떨어진다.

> **역정규화 상수 확정(A2 해소):** `PENALTY_CAP` → `objectivity.OBJ_PENALTY_FULL = 16`. 원안 출발값은
> 감점1호(홍보) 실측 8이었으나, 8을 하한으로 잡으면 medium 위반 1건짜리 기사가 객관성 0이 되어 과하게
> 가혹했다. 16 = medium 2건을 하한으로 두면 medium 1건이 정확히 0.5가 되어 눈금이 자연스럽다.
> 라벨 2개로 잡은 시드값이므로 데이터 축적 후 tier 가중치와 함께 재보정한다(코드에 `ponytail:` 주석으로 표시).

### 5.7 ⑦ 요약 + 평판 갱신

- 최종 선정 기사로 요약 생성(기존 요약 파이프라인 재사용).
- **평판 갱신 (`media.json`):**
  - 클러스터 **멤버**로 등장한 매체: `appear += 1`
  - 클러스터 **대표**로 뽑힌 매체: `win += 1`
  - `선택률 = win / appear`
- **왜 원시 횟수가 아니라 선택률인가:** 통신사는 물량이 많아 `win` 절대값이 커진다. 정규화(선택률)해야 "나온 만큼 대표로 뽑히는 비율"이라는 공정한 신뢰도가 된다(C3와 같은 편향 방지).

---

## 6. 데이터 스키마

모든 파일은 `scores/` 아래. UTF-8, `indent=2`, `ensure_ascii=False`.

### 6.1 `scores/core-word-weights.json` (누적, 단일 파일)

```json
{
  "updated": "2026-07-14",
  "weights": {
    "관세": 1.8,
    "금리": 1.2,
    "반도체": 0.9
  }
}
```
- `weights`: 코어단어 → 누적 가중치(float). ②에서 매일 갱신.
- 파일 없으면 `{"updated": null, "weights": {}}`로 폴백(첫날 = 초기값 기록).

### 6.2 `scores/topics-<date>.json` (일별, 날짜당 1개)

```json
{
  "date": "2026-07-14",
  "topics": [
    {"rank": 1, "core_words": ["관세", "상호관세"], "media_count": 7, "article_count": 12, "score": 9.4},
    {"rank": 2, "core_words": ["금리", "기준금리"], "media_count": 5, "article_count": 6,  "score": 6.2},
    {"rank": 3, "core_words": ["반도체"],           "media_count": 4, "article_count": 4,  "score": 4.8}
  ]
}
```
- `score = media_count + article_count × 0.2`
- top-3만 기록. 감사·재현용(그날 왜 이 주제가 top이었는지).

### 6.3 `scores/media.json` (기존 파일에 필드 추가)

기존 density 필드는 그대로 두고 평판 3필드 추가:

```json
{
  "연합뉴스": {
    "…기존 density 필드…": "그대로 유지",
    "appear": 120,
    "win": 18,
    "selection_rate": 0.15
  }
}
```
- `appear`/`win`: 정수 누적. `selection_rate = win / appear` (appear=0이면 `null` 또는 0).
- **하위호환:** 기존 매체 엔트리에 3필드 없으면 최초 갱신 시 0에서 시작. `compute_ranks`는 이 필드를 무시하므로 density 파생에 영향 없음.

---

## 7. 결정 · 트레이드오프

| 결정 | 채택 | 버린 대안 | 트레이드오프 |
|---|---|---|---|
| D1 매체 서열 | `compute_ranks` density 런타임 파생 | `sources.yaml` 수동 우선순위 | 수동 유지비 0 · 초기 데이터 부족 시 서열이 흔들릴 수 있음(누적으로 안정화) |
| D2 코어단어 | 제목 자동 추출 | 수동 키워드 사전 | 유지비 0 · 추출 오탐 가능(불용어·짧은 단어) → 정규화로 완화 |
| D3 평판 지표 | 선택률(win/appear) | 원시 win 횟수 | 통신사 물량 편향 제거 · 표본 적을 때 분산 큼(appear 하한 고려 여지) |
| D4 근거성 | **독립 출처 커버리지**(누가 밝혔나) | 근거 신호 총량 → 문장 커버리지(구현 중 재변경, §5.6) | 홍보 자기인용 방어 · 출처 표지 없이 사실만 쓴 기사가 저평가될 여지 |
| D5 길이 필터 위치 | ⑥ 직전 지연 적용 | 전 후보 선(先) 필터 | 본문 추출 비용 폭증 방지 · 정렬 후보에 미추출 기사 섞임(로그로 추적) |
| D6 채점 가중치 | 객관성 0.6 / 근거성 0.4 | 5:5 등 | 서비스 목적(객관성 우위) 반영 · 근거 약한 객관 기사가 유리해질 수 있음(허용) |
| D7 주제 점수 | 매체수 주가중(+기사수×0.2) | 기사수 주가중 | 다양성 > 물량 · 소수 매체 특종이 저평가될 수 있음(허용) |

---

## 8. 검증 · Phase 로드맵

### 8.1 검증 방법 (공통)

- **라벨 픽스처 회귀:** `tests/fixtures/label_naver_088.txt`(예시1호·객관), `label_zdnet_esg.txt`(감점1호·홍보).
  - 예시1호: 채점 통과(대표로 선정될 수 있어야 함).
  - 감점1호: 근거밀도가 높아도(3.59) 객관성 감점(8)으로 대표 경쟁에서 밀려야 함.
- **골든 파일:** 특정 `raw/<date>.json` 입력 → `selected/<date>.json` 출력 스냅샷 비교.
- **기존 123 테스트 그린 유지** — 파이프라인 교체가 회귀를 내지 않는지 게이트.

### 8.2 Phase 로드맵

| Phase | 범위 | 완료 조건 | 상태 |
|---|---|---|---|
| **0** | 채점 기반 함수 | `body_objectivity`·`source_coverage`·`evidence_signals` + 라벨 실측 | ✅ 완료 (`5882251`·`f29c473`·`20d29ae`·`8877518`) |
| **1** | ① 2단계 클러스터링 | 매체 내부→매체 간 클러스터, 코어단어 공유 보강. 단위 테스트 | ✅ 완료 (`646917e`, `test_two_stage_cluster`) |
| **2** | ② 코어단어·주제·가중치 | `topics-<date>.json`·`core-word-weights.json` 생성·누적. 첫날 초기값·decay 결정 | ✅ 완료 (`819100d`·`adad81f`, `test_core_words`) — decay는 미도입(A1) |
| **3** | ③④⑤ 길이·정렬·백필 | 지연 길이 필터(이탈 로그) + 3키 정렬 + density 라운드로빈 백필 | ✅ 완료 (`36f7d13`·`0bc5d3b`, `test_representative_backfill`) |
| **4** | ⑥ 대표 채점표 | 0.6/0.4 채점, 역정규화 상수 라벨 보정, 동점=density. 픽스처 회귀 통과 | ✅ 완료 (`fd6317c`·`06b2216`) — 근거성을 독립 출처 커버리지로 변경(§5.6) |
| **5** | ⑦ 평판 갱신 | `media.json` appear/win/선택률 누적, `compute_ranks` 무영향 확인 | ✅ 완료 (`b378888`·`255c2b8`, `test_selection_rate`) |
| **6** | 통합·라이브 | 전 파이프라인 결선, 골든 파일 확정, 브랜치 병합·라이브 반영 | ⬜ **← 현재 위치** |

> 병목 주의: Phase 3의 **지연 길이 필터**가 파이프라인 실행 순서(논리 ③ / 실행 ⑥)를 꼬이게 하기 쉽다. 정렬·백필로 후보를 좁힌 뒤에만 본문을 추출한다는 불변식을 코드 주석과 테스트로 못박을 것.

### 8.3 예외 처리 (침묵 금지)

| 상황 | 시스템 동작 | 로그/표시 |
|---|---|---|
| 대표 후보 전원 길이 이탈 | 차선 멤버로 재시도 → 없으면 클러스터 드롭 | ⚠️ 이탈 사유(짧음/김) 기록 |
| `core-word-weights.json` 없음 | 빈 저장소로 폴백, 첫날 top-3가 초기값 | ℹ️ "가중치 초기화" 로그 |
| 매체가 `media.json`에 없음 | appear/win 0에서 신규 시작 | ℹ️ 신규 매체 등록 로그 |
| 본문 추출 실패 | 그 후보 스킵, 클러스터는 유지 | ⚠️ 추출 실패 로그(failures 연계) |

---

## 부록: 미결 사항 (구현 중 확정)

- A1. 가중치 감쇠(decay) 도입 시점·방식 — **미결 유지.** 가산만으로 운영 중. 값이 포화하면 EMA 도입(§5.2).
  아직 실측 일수가 짧아 판단 근거 부족.
- A2. 역정규화 상수 — ✅ **해소.** `objectivity.OBJ_PENALTY_FULL = 16`(원안 8 → 16). 근거는 §5.6.
- A3. 카테고리 구조 유지 여부 — ✅ **해소: 분야별 상한 유지.** 원안은 전역 상위10이었으나, `select()`가
  `by_cat` 루프 + `limits.yaml` 상한을 그대로 쓰는 쪽으로 구현됐다. 정렬·백필은 **분야 안에서** 돈다.
  이유: 웹·이메일 렌더링이 분야별 표시를 전제하고 있어(비목표 "렌더링 변경 없음") 전역 상위10으로 바꾸면
  범위 밖 변경이 딸려온다.
- A4. `selection_rate` 신뢰 하한(최소 appear 표본 수) — Phase 5 데이터 축적 후.
