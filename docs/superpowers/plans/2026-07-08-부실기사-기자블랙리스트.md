# 부실 기사 기자 블랙리스트 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 본문이 없거나 부실한 기사를 반복 생산하는 기자를 감지·누적하고, 누적 3점 도달 시 그 기자의 기사를 선별에서 제외한다.

**Architecture:** 새 공유 모듈 `src/reporters.py`가 `scores/reporters.json`(기자별 카운트·점수·블랙리스트·history)의 I/O와 점수 로직을 담당한다. `fetch.py`는 RSS `author`를 raw에 저장하고, `curate.py`는 블랙리스트 기자 기사를 선별 후보에서 제외하며, `summarize.py`는 대표 기사 본문 품질을 판정해 스트라이크를 기록한다. 대표 본문 추출은 `extract_body`에 per-run 캐시를 태워 이중 추출을 막는다.

**Tech Stack:** Python 3(stdlib: json/pathlib/datetime), feedparser, PyYAML, BeautifulSoup4, requests, replicate. 테스트는 표준 `unittest`(pytest 아님).

## Global Constraints

- 설계 정본: [docs/superpowers/specs/2026-07-08-부실기사-기자블랙리스트-design.md](../specs/2026-07-08-부실기사-기자블랙리스트-design.md). 충돌 시 스펙 우선.
- 점수 규칙: 본문 없음(extract_body→None) = 즉시 **+1점**; 부실(본문 길이 **80 이상 200 미만**) = sparse_count **+1**, 3 도달 시 +1점·sparse_count 0으로 리셋; 정상(≥200) = 변화 없음. **points ≥ 3 → blacklisted = true**.
- 키: `"{매체}::{정규화된_기자명}"`. 기자명 정규화 = 앞뒤 공백 제거 + 끝의 `"기자"` 접미사 제거.
- history `(date, link)` 중복이면 재기록하지 않는다(이중 감점 금지).
- `author` 없으면(빈 문자열/필드 없음) 판정·필터 모두 스킵.
- 정상 기사는 sparse_count를 리셋하지 않는다(자동 복권 없음).
- 저장 JSON은 `ensure_ascii=False, indent=2`. 기존 파일 스타일(unittest, `sys.path.insert(0, .../src)`)을 따른다.
- 단일 테스트 파일 실행: `python3 -m unittest discover -s tests -p <파일명> -v`. 전체: `python3 -m unittest discover -s tests`.
- 커밋은 각 Task 끝에서. 브랜치 `feat/reporter-blacklist`에서 작업.

---

### Task 1: `src/reporters.py` — 기자 스트라이크 공유 모듈

**Files:**
- Create: `src/reporters.py`
- Test: `tests/test_reporters.py`

**Interfaces:**
- Produces:
  - `normalize_author(name: str) -> str`
  - `reporter_key(source: str, author: str) -> str`
  - `classify_body(body: str | None) -> str | None`  # "empty" | "sparse" | None
  - `load() -> dict` / `save(data: dict) -> None`
  - `blacklisted_keys(data: dict) -> set[str]`
  - `record_strike(data: dict, source: str, author: str, date: str, link: str, reason: str, chars: int) -> dict`
  - 모듈 상수 `REPORTERS_FILE: Path`(테스트에서 patch 가능)

- [ ] **Step 1: Write the failing test**

`tests/test_reporters.py`:

```python
"""기자 부실 스트라이크 모듈 테스트 (네트워크/API 미사용)."""

import json
import os
import sys
import tempfile
import unittest
from pathlib import Path
from unittest.mock import patch

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

import reporters  # noqa: E402


class NormalizeAuthorTest(unittest.TestCase):
    def test_strips_reporter_suffix(self):
        self.assertEqual(reporters.normalize_author("김송이 기자"), "김송이")

    def test_trims_whitespace(self):
        self.assertEqual(reporters.normalize_author("  황철환  "), "황철환")

    def test_plain_name_unchanged(self):
        self.assertEqual(reporters.normalize_author("이원지"), "이원지")


class ReporterKeyTest(unittest.TestCase):
    def test_key_scopes_by_source(self):
        self.assertEqual(reporters.reporter_key("연합뉴스", "황철환 기자"), "연합뉴스::황철환")


class ClassifyBodyTest(unittest.TestCase):
    def test_none_is_empty(self):
        self.assertEqual(reporters.classify_body(None), "empty")

    def test_short_body_is_sparse(self):
        self.assertEqual(reporters.classify_body("가" * 150), "sparse")

    def test_boundary_200_is_ok(self):
        self.assertIsNone(reporters.classify_body("가" * 200))

    def test_long_body_is_ok(self):
        self.assertIsNone(reporters.classify_body("가" * 500))


class RecordStrikeTest(unittest.TestCase):
    def test_empty_adds_one_point(self):
        data = reporters.record_strike({}, "연합뉴스", "황철환", "2026-07-08", "L1", "empty", 0)
        rec = data["연합뉴스::황철환"]
        self.assertEqual(rec["points"], 1)
        self.assertFalse(rec["blacklisted"])

    def test_three_empties_blacklist(self):
        data = {}
        for i in range(3):
            data = reporters.record_strike(data, "연합뉴스", "황철환", "2026-07-08",
                                           f"L{i}", "empty", 0)
        self.assertTrue(data["연합뉴스::황철환"]["blacklisted"])

    def test_three_sparse_make_one_point_and_reset(self):
        data = {}
        for i in range(3):
            data = reporters.record_strike(data, "연합뉴스", "황철환", "2026-07-08",
                                           f"L{i}", "sparse", 120)
        rec = data["연합뉴스::황철환"]
        self.assertEqual(rec["points"], 1)
        self.assertEqual(rec["sparse_count"], 0)

    def test_two_sparse_no_point_yet(self):
        data = {}
        for i in range(2):
            data = reporters.record_strike(data, "연합뉴스", "황철환", "2026-07-08",
                                           f"L{i}", "sparse", 120)
        rec = data["연합뉴스::황철환"]
        self.assertEqual(rec["points"], 0)
        self.assertEqual(rec["sparse_count"], 2)

    def test_duplicate_date_link_is_noop(self):
        data = reporters.record_strike({}, "연합뉴스", "황철환", "2026-07-08", "L1", "empty", 0)
        data = reporters.record_strike(data, "연합뉴스", "황철환", "2026-07-08", "L1", "empty", 0)
        rec = data["연합뉴스::황철환"]
        self.assertEqual(rec["points"], 1)
        self.assertEqual(len(rec["history"]), 1)

    def test_history_records_reason_and_chars(self):
        data = reporters.record_strike({}, "연합뉴스", "황철환", "2026-07-08", "L1", "sparse", 123)
        h = data["연합뉴스::황철환"]["history"][0]
        self.assertEqual(h, {"date": "2026-07-08", "link": "L1",
                             "reason": "sparse", "chars": 123})


class BlacklistedKeysTest(unittest.TestCase):
    def test_returns_only_blacklisted(self):
        data = {
            "A::x": {"blacklisted": True, "points": 3, "sparse_count": 0, "history": []},
            "B::y": {"blacklisted": False, "points": 1, "sparse_count": 0, "history": []},
        }
        self.assertEqual(reporters.blacklisted_keys(data), {"A::x"})


class LoadSaveTest(unittest.TestCase):
    def test_missing_file_returns_empty(self):
        with tempfile.TemporaryDirectory() as tmp:
            with patch.object(reporters, "REPORTERS_FILE", Path(tmp) / "reporters.json"):
                self.assertEqual(reporters.load(), {})

    def test_roundtrip(self):
        with tempfile.TemporaryDirectory() as tmp:
            with patch.object(reporters, "REPORTERS_FILE", Path(tmp) / "reporters.json"):
                reporters.save({"연합뉴스::황철환": {"points": 1}})
                self.assertEqual(reporters.load(), {"연합뉴스::황철환": {"points": 1}})


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m unittest discover -s tests -p test_reporters.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'reporters'`

- [ ] **Step 3: Write minimal implementation**

`src/reporters.py`:

```python
"""기자 부실 스트라이크 축적 (본문 없음/부실 반복 기자 감지·제외).

매체 평판(scores/media.json)이 '매체' 단위라면, 이 모듈은 '기자' 단위 부실 이력을
scores/reporters.json에 누적한다. 점수 3점 도달 시 blacklisted=true가 되고,
curate가 해당 기자 기사를 선별 후보에서 제외한다.

설계: docs/superpowers/specs/2026-07-08-부실기사-기자블랙리스트-design.md
"""

from __future__ import annotations

import json
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
REPORTERS_FILE = ROOT / "scores" / "reporters.json"

SPARSE_MIN_CHARS = 200   # 본문 길이가 이 값 미만이면 '부실'
SPARSE_TO_POINT = 3      # 부실 카운트가 이 값에 도달하면 1점으로 승격
BLACKLIST_POINTS = 3     # 누적 점수가 이 값 이상이면 블랙리스트


def normalize_author(name: str) -> str:
    """기자명 정규화: 앞뒤 공백 제거 + 끝의 '기자' 접미사 제거."""
    n = name.strip()
    if n.endswith("기자"):
        n = n[:-2].strip()
    return n


def reporter_key(source: str, author: str) -> str:
    """'{매체}::{정규화된 기자명}' 키."""
    return f"{source}::{normalize_author(author)}"


def classify_body(body: str | None) -> str | None:
    """대표 기사 본문 품질을 판정한다. 'empty' | 'sparse' | None(정상)."""
    if body is None:
        return "empty"
    if len(body) < SPARSE_MIN_CHARS:
        return "sparse"
    return None


def load() -> dict:
    if not REPORTERS_FILE.exists():
        return {}
    with REPORTERS_FILE.open("r", encoding="utf-8") as f:
        return json.load(f)


def save(data: dict) -> None:
    REPORTERS_FILE.parent.mkdir(parents=True, exist_ok=True)
    with REPORTERS_FILE.open("w", encoding="utf-8") as f:
        json.dump(data, f, ensure_ascii=False, indent=2)


def blacklisted_keys(data: dict) -> set[str]:
    return {k for k, v in data.items() if v.get("blacklisted")}


def record_strike(data: dict, source: str, author: str, date: str,
                  link: str, reason: str, chars: int) -> dict:
    """대표 기사 판정 결과(reason='empty'|'sparse')를 기자 이력에 반영한다.

    같은 (date, link)가 이미 있으면 no-op(이중 감점 금지). 갱신된 data를 반환한다.
    """
    key = reporter_key(source, author)
    rec = data.setdefault(
        key, {"sparse_count": 0, "points": 0, "blacklisted": False,
              "last_updated": "", "history": []},
    )
    if any(h["date"] == date and h["link"] == link for h in rec["history"]):
        return data

    rec["history"].append({"date": date, "link": link, "reason": reason, "chars": chars})
    rec["last_updated"] = date
    if reason == "empty":
        rec["points"] += 1
    elif reason == "sparse":
        rec["sparse_count"] += 1
        if rec["sparse_count"] >= SPARSE_TO_POINT:
            rec["points"] += 1
            rec["sparse_count"] = 0
    if rec["points"] >= BLACKLIST_POINTS:
        rec["blacklisted"] = True
    return data
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m unittest discover -s tests -p test_reporters.py -v`
Expected: PASS (all cases)

- [ ] **Step 5: Commit**

```bash
git add src/reporters.py tests/test_reporters.py
git commit -m "feat: 기자 부실 스트라이크 모듈(reporters.py)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: `fetch.py` — RSS author를 raw에 저장

**Files:**
- Modify: `src/fetch.py` (`fetch_feed`의 article dict)
- Test: `tests/test_fetch_author.py` (신규)

**Interfaces:**
- Consumes: 없음(feedparser entry).
- Produces: raw 기사 dict에 `"author"` 필드(문자열, 없으면 `""`). curate/summarize가 사용.

- [ ] **Step 1: Write the failing test**

`tests/test_fetch_author.py`:

```python
"""fetch가 RSS author를 raw 기사에 저장하는지 검증 (네트워크 미사용, feedparser mock)."""

import os
import sys
import unittest
from types import SimpleNamespace
from unittest.mock import patch

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

import fetch  # noqa: E402


class FetchAuthorTest(unittest.TestCase):
    def _parsed(self, entry):
        return SimpleNamespace(bozo=0, entries=[entry])

    def test_author_is_captured(self):
        entry = {"title": "제목", "link": "http://x/1", "published": "",
                 "summary": "요약", "author": "황철환 기자"}
        with patch("fetch.feedparser.parse", return_value=self._parsed(entry)):
            arts = fetch.fetch_feed("경제", {"매체명": "연합뉴스", "url": "u"}, "now")
        self.assertEqual(arts[0]["author"], "황철환 기자")

    def test_missing_author_is_empty_string(self):
        entry = {"title": "제목", "link": "http://x/2", "published": "",
                 "summary": "요약"}
        with patch("fetch.feedparser.parse", return_value=self._parsed(entry)):
            arts = fetch.fetch_feed("경제", {"매체명": "연합뉴스", "url": "u"}, "now")
        self.assertEqual(arts[0]["author"], "")


if __name__ == "__main__":
    unittest.main()
```

Note: feedparser entries는 `entry.get(...)`를 지원하는 dict-like다. 위 테스트는 평범한 dict로 대체해 `entry.get("author", "")` 경로를 검증한다.

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m unittest discover -s tests -p test_fetch_author.py -v`
Expected: FAIL — `KeyError: 'author'` (article dict에 author 키 없음)

- [ ] **Step 3: Write minimal implementation**

`src/fetch.py`의 `fetch_feed` 내부 article dict에 `author` 한 줄 추가:

```python
        articles.append(
            {
                "title": html.unescape(entry.get("title", "")).strip(),
                "link": entry.get("link", "").strip(),
                "published": entry.get("published", entry.get("updated", "")),
                "published_iso": _to_iso(entry),
                "summary": html.unescape(entry.get("summary", "")).strip(),
                "author": entry.get("author", "").strip(),
                "source": name,
                "category": category,
                "fetched_at": fetched_at,
            }
        )
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m unittest discover -s tests -p test_fetch_author.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/fetch.py tests/test_fetch_author.py
git commit -m "feat: RSS author를 raw 기사에 저장

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: `curate.py` — 블랙리스트 기자 기사 선별 제외

**Files:**
- Modify: `src/curate.py` (import reporters, `filter_blacklisted` 추가, `main`에서 필터 적용)
- Test: `tests/test_curate_blacklist.py` (신규)

**Interfaces:**
- Consumes: `reporters.reporter_key`, `reporters.load`, `reporters.blacklisted_keys` (Task 1).
- Produces: `curate.filter_blacklisted(articles: list[dict], blacklist: set[str]) -> list[dict]`.

- [ ] **Step 1: Write the failing test**

`tests/test_curate_blacklist.py`:

```python
"""블랙리스트 기자 기사가 선별 후보에서 제외되는지 검증 (네트워크/API 미사용)."""

import os
import sys
import unittest

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

import curate  # noqa: E402


def _art(source, author):
    return {"category": "경제", "source": source, "author": author,
            "title": f"{source}-{author}", "summary": "", "link": f"L-{source}-{author}",
            "published_iso": "2026-07-08T01:00:00+00:00"}


class FilterBlacklistedTest(unittest.TestCase):
    def test_blacklisted_reporter_removed(self):
        arts = [_art("연합뉴스", "황철환 기자"), _art("연합뉴스", "김리안")]
        blacklist = {"연합뉴스::황철환"}
        out = curate.filter_blacklisted(arts, blacklist)
        self.assertEqual([a["author"] for a in out], ["김리안"])

    def test_empty_blacklist_keeps_all(self):
        arts = [_art("연합뉴스", "황철환 기자")]
        self.assertEqual(curate.filter_blacklisted(arts, set()), arts)

    def test_article_without_author_passes(self):
        art = {"category": "경제", "source": "연합뉴스", "title": "무저자",
               "summary": "", "link": "L", "published_iso": ""}
        out = curate.filter_blacklisted([art], {"연합뉴스::황철환"})
        self.assertEqual(out, [art])

    def test_same_name_other_source_not_filtered(self):
        arts = [_art("한국경제", "황철환 기자")]  # 블랙은 연합뉴스::황철환뿐
        out = curate.filter_blacklisted(arts, {"연합뉴스::황철환"})
        self.assertEqual(len(out), 1)


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m unittest discover -s tests -p test_curate_blacklist.py -v`
Expected: FAIL — `AttributeError: module 'curate' has no attribute 'filter_blacklisted'`

- [ ] **Step 3: Write minimal implementation**

`src/curate.py` 상단 import에 추가:

```python
import reporters
```

`load_raw` 아래(또는 `select` 위)에 함수 추가:

```python
def filter_blacklisted(articles: list[dict], blacklist: set[str]) -> list[dict]:
    """블랙리스트 (매체,기자) 기사를 후보에서 제거한다.

    author 필드가 없거나 빈 기사는 키가 블랙리스트에 없으므로 통과한다(안전).
    """
    if not blacklist:
        return articles
    return [
        a for a in articles
        if reporters.reporter_key(a["source"], a.get("author", "")) not in blacklist
    ]
```

`main()`에서 `select()` 호출 전에 필터를 적용한다. 현재:

```python
    priority_map = load_priority_map(SOURCES_FILE)
    default_limit, per_category_limits = load_limits(LIMITS_FILE)
    today = datetime.now(KST)
    result = select(raw, priority_map, today, default_limit, per_category_limits)
```

를 다음으로 바꾼다:

```python
    priority_map = load_priority_map(SOURCES_FILE)
    default_limit, per_category_limits = load_limits(LIMITS_FILE)
    blacklist = reporters.blacklisted_keys(reporters.load())
    raw["articles"] = filter_blacklisted(raw["articles"], blacklist)
    today = datetime.now(KST)
    result = select(raw, priority_map, today, default_limit, per_category_limits)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m unittest discover -s tests -p test_curate_blacklist.py -v`
Expected: PASS

기존 회귀 없음 확인:
Run: `python3 -m unittest discover -s tests -p test_curate_limits.py -v`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/curate.py tests/test_curate_blacklist.py
git commit -m "feat: 블랙리스트 기자 기사를 선별 후보에서 제외

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: `extract.py` — 대표 본문 이중 추출 방지용 per-run 캐시

**Files:**
- Modify: `src/extract.py` (`extract_body`, `iter_contents`에 optional `cache` 파라미터)
- Test: `tests/test_extract.py` (케이스 추가)

**Interfaces:**
- Produces:
  - `extract_body(url: str, title: str = "", cache: dict | None = None) -> str | None`
  - `iter_contents(item: dict, cache: dict | None = None)` — 내부에서 `extract_body(link, title, cache=cache)` 호출.
- 캐시는 `{url: (str|None)}`. `cache=None`이면 기존 동작(항상 새로 추출).

- [ ] **Step 1: Write the failing test**

`tests/test_extract.py` 끝(맨 아래 `if __name__` 위)에 클래스 추가. 파일 상단에 `from unittest.mock import patch`가 없으면 추가한다.

```python
class ExtractBodyCacheTest(unittest.TestCase):
    def test_cache_prevents_second_http_call(self):
        html = "<html><body><article><p>" + ("가" * 300) + "</p></article></body></html>"
        cache = {}
        with patch("extract.requests.get") as m_get:
            m_get.return_value.text = html
            m_get.return_value.raise_for_status = lambda: None
            first = extract.extract_body("http://x/1", "가", cache=cache)
            second = extract.extract_body("http://x/1", "가", cache=cache)
        self.assertEqual(first, second)
        self.assertEqual(m_get.call_count, 1)  # 두 번째는 캐시 히트

    def test_none_result_is_cached(self):
        cache = {}
        with patch("extract.requests.get") as m_get:
            m_get.return_value.text = "<html><body><p>짧음</p></body></html>"
            m_get.return_value.raise_for_status = lambda: None
            extract.extract_body("http://x/2", "제목없는키워드", cache=cache)
            extract.extract_body("http://x/2", "제목없는키워드", cache=cache)
        self.assertIn("http://x/2", cache)
        self.assertIsNone(cache["http://x/2"])
        self.assertEqual(m_get.call_count, 1)
```

`tests/test_extract.py`가 `import extract`와 `sys.path.insert`를 이미 갖고 있다고 가정한다(기존 테스트 파일). 없으면 상단에 추가한다.

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m unittest discover -s tests -p test_extract.py -v`
Expected: FAIL — `extract_body()` got an unexpected keyword argument `cache`

- [ ] **Step 3: Write minimal implementation**

`src/extract.py`의 `extract_body`를 캐시 지원으로 바꾼다:

```python
def extract_body(url: str, title: str = "", cache: dict | None = None) -> str | None:
    """원문 링크에서 본문 텍스트를 추출한다(도메인 선택자+가드). 실패하면 None.

    cache가 주어지면 url 단위로 결과(None 포함)를 기억해 같은 run에서 재추출을 막는다.
    """
    if cache is not None and url in cache:
        return cache[url]
    try:
        resp = requests.get(url, headers={"User-Agent": USER_AGENT}, timeout=TIMEOUT)
        resp.raise_for_status()
    except requests.RequestException:
        result = None
    else:
        result = _parse_body(resp.text, url, title)
    if cache is not None:
        cache[url] = result
    return result
```

`iter_contents`가 캐시를 전달하도록 수정:

```python
def iter_contents(item: dict, cache: dict | None = None):
    ...(독스트링 유지)...
    title = item.get("title", "")
    for source, link, summary in candidate_sources(item):
        body = extract_body(link, title, cache=cache)
        if body:
            yield {"text": body, "content_source": source, "method": "body", "link": link}
        if summary and len(summary) >= MIN_SUMMARY:
            yield {"text": summary, "content_source": source, "method": "rss", "link": link}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m unittest discover -s tests -p test_extract.py -v`
Expected: PASS (기존 케이스 포함 전부)

- [ ] **Step 5: Commit**

```bash
git add src/extract.py tests/test_extract.py
git commit -m "feat: extract_body per-run 캐시(대표 본문 이중 추출 방지)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 5: `summarize.py` — 대표 기사 판정 → 스트라이크 기록

**Files:**
- Modify: `src/summarize.py` (import, `summarize_item`에 `body_cache` 전달, `run()`에 판정·기록·저장)
- Test: `tests/test_reporter_strike.py` (신규)

**Interfaces:**
- Consumes: `reporters.load/save/record_strike/classify_body`(Task 1), `extract_body`(Task 4).
- Produces: run() 실행 시 `scores/reporters.json` 갱신(스트라이크가 있을 때만 저장).

- [ ] **Step 1: Write the failing test**

`tests/test_reporter_strike.py`:

```python
"""run()이 대표 기사 본문을 판정해 기자 스트라이크를 기록하는지 검증."""

import os
import sys
import tempfile
import unittest
from pathlib import Path
from unittest.mock import patch

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

import reporters  # noqa: E402
import summarize  # noqa: E402


class ReporterStrikeInRunTest(unittest.TestCase):
    def _selected(self):
        return {"date": "2026-07-08", "categories": {"경제": [
            {"title": "제목", "source": "연합뉴스", "author": "황철환 기자",
             "link": "L1", "related_links": []},
        ]}}

    def _run(self, tmp, extract_return):
        # summarize_item은 ok를 반환하도록 mock(요약 자체는 관심사 아님).
        with patch.dict(os.environ, {"REPLICATE_API_TOKEN": "x"}), \
             patch("summarize.load_selected", return_value=self._selected()), \
             patch("summarize.load_cache", return_value={}), \
             patch("summarize.save_cache"), \
             patch("summarize.save_failure_log"), \
             patch("summarize.summarize_item",
                   return_value=(["사실"], "연합뉴스", "ok", False, None)), \
             patch("summarize.extract_body", return_value=extract_return), \
             patch.object(summarize, "NEWS_DIR", Path(tmp)), \
             patch.object(summarize, "WEB_DATA_DIR", Path(tmp) / "web"), \
             patch.object(reporters, "REPORTERS_FILE", Path(tmp) / "reporters.json"):
            summarize.run("2026-07-08", dry_run=False)
        return reporters.load()

    def test_empty_body_records_point(self):
        with tempfile.TemporaryDirectory() as tmp:
            data = self._run(tmp, None)  # extract_body None → empty
        self.assertEqual(data["연합뉴스::황철환"]["points"], 1)

    def test_sparse_body_records_count(self):
        with tempfile.TemporaryDirectory() as tmp:
            data = self._run(tmp, "가" * 100)  # 100자 < 200 → sparse
        rec = data["연합뉴스::황철환"]
        self.assertEqual(rec["sparse_count"], 1)
        self.assertEqual(rec["points"], 0)

    def test_normal_body_records_nothing(self):
        with tempfile.TemporaryDirectory() as tmp:
            data = self._run(tmp, "가" * 300)  # ≥200 → ok, 기록 없음
        self.assertEqual(data, {})  # 저장 파일 없음 → load() = {}


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python3 -m unittest discover -s tests -p test_reporter_strike.py -v`
Expected: FAIL — `AttributeError: <module 'summarize'> does not have the attribute 'extract_body'` (아직 import 안 함)

- [ ] **Step 3: Write minimal implementation**

`src/summarize.py` import 수정 — 기존:

```python
from extract import iter_contents
from failure_log import save_failure_log
```

를:

```python
import reporters
from extract import extract_body, iter_contents
from failure_log import save_failure_log
```

`summarize_item` 시그니처와 iter_contents 호출에 `body_cache` 추가 — 기존:

```python
def summarize_item(item: dict, cache: dict, dry_run: bool):
    ...
    had_content = False
    last_error = None
    for content in iter_contents(item):
```

를:

```python
def summarize_item(item: dict, cache: dict, dry_run: bool, body_cache: dict | None = None):
    ...
    had_content = False
    last_error = None
    for content in iter_contents(item, cache=body_cache):
```

(독스트링은 유지. 기존 호출부 `summarize_item(item, cache, dry_run)`는 positional 3개라 영향 없음.)

`run()`의 요약 루프를 수정한다. 기존:

```python
    cache = load_cache()
    results: dict[str, tuple[list[str], str]] = {}
    counters = {"ok": 0, "api_failed": 0, "call_error": 0,
                "extract_failed": 0, "cached": 0}
    failures = []

    for category, items in selected["categories"].items():
        for item in items:
            bullets, source, status, cached, detail = summarize_item(item, cache, dry_run)
```

를:

```python
    cache = load_cache()
    rep_data = reporters.load()
    rep_dirty = False
    body_cache: dict = {}
    results: dict[str, tuple[list[str], str]] = {}
    counters = {"ok": 0, "api_failed": 0, "call_error": 0,
                "extract_failed": 0, "cached": 0}
    failures = []

    for category, items in selected["categories"].items():
        for item in items:
            bullets, source, status, cached, detail = summarize_item(
                item, cache, dry_run, body_cache)

            # 기자 부실 스트라이크: 대표 기사 본문 품질을 판정해 누적(3점 → 블랙리스트).
            # 요약 성패와 무관하게 대표 기사 자체를 판정한다(본문 없음은 실패의 원인이기도 함).
            if not dry_run and item.get("author"):
                rep_body = extract_body(item["link"], item.get("title", ""), cache=body_cache)
                reason = reporters.classify_body(rep_body)
                if reason:
                    reporters.record_strike(
                        rep_data, item["source"], item["author"], date,
                        item["link"], reason, 0 if rep_body is None else len(rep_body))
                    rep_dirty = True
```

`run()`에서 캐시 저장 부근에 reporters 저장 추가. 기존:

```python
    if not dry_run:
        save_cache(cache)
```

를:

```python
    if not dry_run:
        save_cache(cache)
        if rep_dirty:
            reporters.save(rep_data)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python3 -m unittest discover -s tests -p test_reporter_strike.py -v`
Expected: PASS

기존 요약/실행 회귀 없음 확인:
Run: `python3 -m unittest discover -s tests -p test_fallback.py -v`
Expected: PASS (특히 RunExitCodeTest — 무저자 selected라 스트라이크·저장 스킵)

- [ ] **Step 5: Commit**

```bash
git add src/summarize.py tests/test_reporter_strike.py
git commit -m "feat: 대표 기사 본문 판정으로 기자 스트라이크 기록

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 6: 전체 회귀 + 문서 갱신

**Files:**
- Modify: `README.md`(파이프라인 구조에 reporters 한 줄), 필요 시 `.gitignore` 확인(scores/ 추적 유지).

- [ ] **Step 1: 전체 테스트**

Run: `python3 -m unittest discover -s tests`
Expected: OK (전부 통과)

- [ ] **Step 2: scores/reporters.json 추적 정책 확인**

Run: `git check-ignore scores/reporters.json || echo "TRACKED-OK"`
Expected: `TRACKED-OK` — reporters.json은 커밋 대상(scores/media.json 선례처럼 누적 히스토리 자산).
`.gitignore`가 `scores/`를 무시하면 예외를 추가한다(`!scores/reporters.json`).

- [ ] **Step 3: README 파이프라인 설명에 한 줄 추가**

`README.md`의 파이프라인 구조 다이어그램 아래에 다음 문장을 추가:

```markdown
> 부실 기사(본문 없음/한두 문장)를 반복 내는 기자는 `scores/reporters.json`에 누적되어
> 3점 도달 시 `curate` 선별에서 제외된다(설계: docs/superpowers/specs/2026-07-08-부실기사-기자블랙리스트-design.md).
```

- [ ] **Step 4: Commit**

```bash
git add README.md
git commit -m "docs: 기자 블랙리스트 파이프라인 설명 추가

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review 결과

**Spec coverage:**
- §2 점수 규칙 → Task 1(record_strike/classify_body), Task 5(run 판정) ✔
- §3 키·정규화 → Task 1(normalize_author/reporter_key) ✔
- §4 저장·history·중복방지 → Task 1(load/save/record_strike dedup) ✔
- §5.1 reporters.py → Task 1 ✔ / §5.2 fetch author → Task 2 ✔ / §5.3 curate 필터 → Task 3 ✔ / §5.4 summarize 판정·이중추출 방지 → Task 4(캐시)+Task 5 ✔
- §6 즉시 적용 → Task 5(관찰전용 게이트 없음) ✔
- §7 엣지(author 없음 스킵·옛 raw 통과·중복 방지·blacklist 후 판정중단) → Task 1/3/5 테스트로 커버 ✔
- §8 테스트(reporters/curate/fetch) → Task 1/2/3, 추가로 Task 5 통합 ✔

**Placeholder scan:** 없음(모든 코드 스텝에 실제 코드/명령/기대출력 포함).

**Type consistency:** `reporter_key`·`classify_body`·`record_strike`·`blacklisted_keys` 시그니처가 Task 1 정의와 Task 3/5 사용에서 일치. `extract_body(url, title, cache=None)`·`iter_contents(item, cache=None)`가 Task 4 정의와 Task 5 호출에서 일치. `record_strike` reason 값 `"empty"|"sparse"`가 `classify_body` 반환과 일치.
