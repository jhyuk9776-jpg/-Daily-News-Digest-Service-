# 객관성 변별력 그물망 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 매체 객관성 채점을 "감점 밀도(1000건당 감점 point)"로 전환하고 구조·어휘·인용·교차 신호를 더해, 지금 99.8~100에 몰린 매체 점수에 변별력을 만든다. observe-only(다이제스트 파이프라인 불변).

**Architecture:** `penalties.yaml`에 `scoring:` 튜너블 블록·`scope` 필드·구조/어휘 항목·인용 표지를 추가한다. `src/objectivity.py`는 채널별(제목/리드/본문) 채점 + 등급 weight + 가속 + 차등가중 + 관찰축(인용·교차 이상치)으로 개편하고, 집계를 EWMA 평균에서 누적 감점밀도로 바꾼다. 트랙2(다이제스트 본문 감사)는 별도 모듈 `src/digest_audit.py`로 격리한다.

**Tech Stack:** Python 3.9, PyYAML, 표준 `unittest`(네트워크/AI 미사용). 실행: `python3 -m unittest tests.test_objectivity -v`.

## Global Constraints

- observe-only 불변: `src/curate.py`, `src/summarize.py`, `run.sh`, `.github/workflows/daily.yml` **미변경**. (검증 대상)
- 비용 0: 채점은 AI 토큰 미사용. 트랙2 본문 추출은 `extract.py`(BeautifulSoup)만 사용, 최대 16건/일.
- 튜너블 상수는 전부 **시드값(데이터 후 보정)**: `tiers{strong:15,medium:8,weak:3}`, `escalation{T:3,step:5,cap:45}`, `body_factor:0.5`, `BASELINE=100`.
- 밀도 방향: **낮을수록 객관적**. 매체 밀도 = `Σ escalated_points / Σ article_count × 1000`.
- 축 분리: 인용 밀도(②)·교차 이상치(④)는 감점 point에 **섞지 않고** 별도 필드로 기록.
- 한글 파일·주석 유지. 기존 테스트 패턴(`unittest`, `sys.path.insert`, `patch.object`) 따름.
- 스키마 변경이므로 완료 후 `python3 src/objectivity.py --backfill`로 `media.json` 리셋 재구축.

---

## File Structure

- `penalties.yaml` (수정) — `scoring:` 블록, `attribution_markers:`, penalties 항목에 `scope`, 구조/어휘 항목 추가.
- `src/objectivity.py` (수정) — 로더 확장(`load_scoring`, `scope`), 채널 채점(`score_article`), 관찰축(`attribution_count`, 교차 이상치), 밀도 집계.
- `src/digest_audit.py` (신규) — 트랙2: 선별분 본문 재추출 + 본문 포함 채점 + `scores/digest-audit-*.json`.
- `tests/test_objectivity.py` (수정) — 밀도·채널·가속·관찰축 테스트로 개편.
- `tests/test_digest_audit.py` (신규) — 트랙2 테스트.
- `.claude/skills/objectivity-score/SKILL.md`, `.claude/skills/daily-briefing/SKILL.md` (수정) — 밀도·관찰축·감사 렌더링.

---

## Task 1: penalties.yaml — scoring 블록·scope·구조/어휘·인용 표지

**Files:**
- Modify: `penalties.yaml`
- Test: `tests/test_objectivity.py::ScoringConfigTest`

**Interfaces:**
- Produces: `penalties.yaml`에 `scoring`(tiers/escalation/body_factor), `attribution_markers`(list[str]), penalties 항목의 선택적 `scope`(title|lead|body|text), 구조 낚시 항목(scope:title, type:regex), 어휘 확장 항목.

- [ ] **Step 1: penalties.yaml 상단에 scoring 블록과 attribution_markers 추가**

기존 헤더 주석 아래, `penalties:` 앞에 삽입:

```yaml
# 튜너블 상수(시드값 — 데이터 축적 후 보정). 코드 수정 없이 여기서 조정.
scoring:
  tiers: {strong: 15, medium: 8, weak: 3}
  escalation: {T: 3, step: 5, cap: 45}   # hit가 T 초과 시 step만큼 볼록 가산, per-기사 cap 상한
  body_factor: 0.5                        # 본문 매칭은 제목/리드의 절반 가중

# 인용·출처 밀도(② 중립 가점 관찰축). 감점 아님 — 등장수만 기록.
attribution_markers:
  - "에 따르면"
  - "라고 밝혔다"
  - "고 밝혔다"
  - "고 말했다"
  - "라고 말했다"
  - "측은"
  - "측이"
  - "당국은"
```

- [ ] **Step 2: 구조적 낚시(①) 항목을 penalties에 추가(scope:title)**

`penalties:` 리스트의 strong 그룹 끝에 추가:

```yaml
  # ── ① 구조적 낚시 (제목 한정, scope:title) ──
  - expr: "\\?$"
    type: regex
    tier: medium
    weight: 8
    scope: title
    근거: "물음표 종결 낚시"
  - expr: "(할까|일까|되나|아니냐|셈인가|것인가)\\??$"
    type: regex
    tier: medium
    weight: 8
    scope: title
    근거: "추측형 종결"
  - expr: "(\\?!|!!+|…)"
    type: regex
    tier: weak
    weight: 3
    scope: title
    근거: "과도한 문장부호"
```

- [ ] **Step 3: 어휘 밀도 확장(③) 항목을 medium/weak 그룹에 추가**

`# ── medium` 그룹에 추가:

```yaml
  - expr: "파문"
    type: phrase
    tier: medium
    weight: 8
    근거: "과장 프레이밍"
  - expr: "발칵"
    type: phrase
    tier: medium
    weight: 8
    근거: "감정 과장"
  - expr: "초강수"
    type: phrase
    tier: medium
    weight: 8
    근거: "드라마화"
```

- [ ] **Step 4: 실패 테스트 작성 — scoring 블록이 로드되는지**

`tests/test_objectivity.py`에 클래스 추가:

```python
class ScoringConfigTest(unittest.TestCase):
    def test_load_scoring_from_real_file(self):
        cfg = objectivity.load_scoring(objectivity.PENALTIES_FILE)
        self.assertEqual(cfg["tiers"]["strong"], 15)
        self.assertEqual(cfg["escalation"]["T"], 3)
        self.assertEqual(cfg["escalation"]["cap"], 45)
        self.assertEqual(cfg["body_factor"], 0.5)
        self.assertIn("에 따르면", cfg["attribution_markers"])
```

- [ ] **Step 5: 테스트 실패 확인**

Run: `python3 -m unittest tests.test_objectivity.ScoringConfigTest -v`
Expected: FAIL — `AttributeError: module 'objectivity' has no attribute 'load_scoring'`

- [ ] **Step 6: 커밋**

```bash
git add penalties.yaml tests/test_objectivity.py
git commit -m "feat(objectivity): penalties.yaml scoring 블록·scope·구조/어휘/인용 확장"
```

---

## Task 2: objectivity.py — load_scoring + scope 파싱

**Files:**
- Modify: `src/objectivity.py`
- Test: `tests/test_objectivity.py::ScoringConfigTest`, `PenaltyLoaderTest`

**Interfaces:**
- Consumes: `penalties.yaml`의 `scoring`, `attribution_markers`, 항목별 `scope`.
- Produces:
  - `load_scoring(path=PENALTIES_FILE) -> dict` = `{"tiers":dict, "escalation":dict, "body_factor":float, "attribution_markers":list[str]}` (파일 없으면 기본 시드).
  - `_normalize_entry` 결과에 `"scope"` 키 포함(기본 `"text"`).
  - 모듈 상수 `SCORING = load_scoring()`.

- [ ] **Step 1: 기본 시드 상수 추가**

`src/objectivity.py`의 `_SEED_PENALTIES` 아래에 추가:

```python
_DEFAULT_SCORING = {
    "tiers": {"strong": 15, "medium": 8, "weak": 3},
    "escalation": {"T": 3, "step": 5, "cap": 45},
    "body_factor": 0.5,
    "attribution_markers": ["에 따르면", "라고 밝혔다", "고 밝혔다", "고 말했다",
                            "라고 말했다", "측은", "측이", "당국은"],
}
```

- [ ] **Step 2: `_normalize_entry`에 scope 추가**

`return {...}` 딕셔너리에 한 줄 추가:

```python
        "scope": entry.get("scope", "text"),
```

- [ ] **Step 3: `load_scoring` 구현**

`load_penalties` 함수 아래에 추가:

```python
def load_scoring(path: Path = PENALTIES_FILE) -> dict:
    """penalties.yaml의 scoring 블록·attribution_markers를 읽는다(없으면 시드)."""
    path = Path(path)
    if not path.exists():
        return {k: (dict(v) if isinstance(v, dict) else list(v))
                for k, v in _DEFAULT_SCORING.items()}
    data = yaml.safe_load(path.read_text(encoding="utf-8")) or {}
    cfg = {k: (dict(v) if isinstance(v, dict) else list(v))
           for k, v in _DEFAULT_SCORING.items()}
    for key in ("tiers", "escalation"):
        cfg[key].update(data.get("scoring", {}).get(key, {}) or {})
    if "scoring" in data and "body_factor" in data["scoring"]:
        cfg["body_factor"] = float(data["scoring"]["body_factor"])
    if data.get("attribution_markers"):
        cfg["attribution_markers"] = list(data["attribution_markers"])
    return cfg
```

- [ ] **Step 4: 모듈 상수 배선**

`ACTIVE_PENALTIES, OBSERVE_PENALTIES, EXCLUSIONS = load_penalties()` 아래에 추가:

```python
SCORING = load_scoring()
```

- [ ] **Step 5: 테스트 실행 — 통과 확인**

Run: `python3 -m unittest tests.test_objectivity.ScoringConfigTest tests.test_objectivity.PenaltyLoaderTest -v`
Expected: PASS (5개)

- [ ] **Step 6: 커밋**

```bash
git add src/objectivity.py
git commit -m "feat(objectivity): load_scoring + 항목 scope 파싱"
```

---

## Task 3: 채널별 채점 — score_article (등급·scope·가속·차등가중)

**Files:**
- Modify: `src/objectivity.py`
- Test: `tests/test_objectivity.py::ScoreTest`, `GradedScoreTest`, `ChannelScoreTest`(신규)

**Interfaces:**
- Consumes: `SCORING`, `ACTIVE_PENALTIES`(scope 포함), `OBSERVE_PENALTIES`.
- Produces:
  - `score_article(channels: dict, penalties=None, observe=None, scoring=None) -> dict`
    반환 `{"points": float, "hits": list[str], "n_hits": int, "observe_hits": list[str], "score": int}`.
    - `channels` 예: `{"title": str, "lead": str, "body": str}` (없는 키는 빈 문자열 취급).
    - scope별 대상 텍스트: `title`→title, `lead`→lead, `body`→body, `text`→title+" "+lead.
    - body 채널 매칭은 `scoring.body_factor` 가중.
    - `raw = Σ(weight × count × factor)`; `n_hits = Σ count(감점만)`;
      `points = min(cap, raw + step × max(0, n_hits − T))`; `score = max(0, 100 − points)`.
  - `objectivity_score(article, penalties=None, observe=None) -> dict`(호환 래퍼): `channels={"title":article.title,"lead":article.summary}`로 `score_article` 호출, 동일 dict 반환.

- [ ] **Step 1: 실패 테스트 작성 — 채널·가속·차등가중**

`tests/test_objectivity.py`에 클래스 추가:

```python
class ChannelScoreTest(unittest.TestCase):
    P = [
        {"expr": "아우성", "type": "phrase", "tier": "medium", "weight": 8,
         "scope": "text", "근거": "감정"},
        {"expr": "\\?$", "type": "regex", "tier": "medium", "weight": 8,
         "scope": "title", "근거": "물음표"},
    ]
    SCORING = {"tiers": {}, "escalation": {"T": 3, "step": 5, "cap": 45},
               "body_factor": 0.5, "attribution_markers": []}

    def test_title_scope_only_matches_title(self):
        # 물음표는 제목에만 감점(scope:title). 리드의 물음표는 무시.
        r = objectivity.score_article(
            {"title": "정말일까?", "lead": "", "body": ""}, self.P, [], self.SCORING)
        self.assertEqual(r["points"], 8)
        r2 = objectivity.score_article(
            {"title": "무역흑자 361억달러", "lead": "정말일까?", "body": ""},
            self.P, [], self.SCORING)
        self.assertEqual(r2["points"], 0)

    def test_body_factor_halves_body_hit(self):
        # 본문의 '아우성'(scope:text는 title+lead만; body는 별도) → body_factor 검증용 body scope
        P = [{"expr": "아우성", "type": "phrase", "tier": "medium", "weight": 8,
              "scope": "body", "근거": "감정"}]
        r = objectivity.score_article(
            {"title": "", "lead": "", "body": "여기저기 아우성"}, P, [], self.SCORING)
        self.assertEqual(r["points"], 4.0)   # 8 × body_factor 0.5

    def test_escalation_above_threshold(self):
        # 5회 등장: raw=40, n_hits=5>T3 → +step*(5-3)=10 → 50, cap45 → 45
        art = {"title": "아우성 아우성 아우성 아우성 아우성", "lead": "", "body": ""}
        r = objectivity.score_article(art, self.P, [], self.SCORING)
        self.assertEqual(r["n_hits"], 5)
        self.assertEqual(r["points"], 45)    # cap
        self.assertEqual(r["score"], 55)

    def test_below_threshold_is_linear(self):
        art = {"title": "아우성 아우성", "lead": "", "body": ""}
        r = objectivity.score_article(art, self.P, [], self.SCORING)
        self.assertEqual(r["points"], 16)    # 2×8, 가속 없음(n=2≤3)
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python3 -m unittest tests.test_objectivity.ChannelScoreTest -v`
Expected: FAIL — `AttributeError: module 'objectivity' has no attribute 'score_article'`

- [ ] **Step 3: `_count_matches`를 텍스트 인자로 일반화 + `score_article` 구현**

기존 `_count_matches(entry, text)`는 유지. 기존 `objectivity_score`를 아래로 **교체**:

```python
def _scope_text(scope: str, channels: dict) -> str:
    if scope == "text":
        return f"{channels.get('title','')} {channels.get('lead','')}"
    return channels.get(scope, "")


def score_article(channels: dict, penalties=None, observe=None, scoring=None) -> dict:
    """기사 1건을 채널별로 채점한다(등급·scope·가속·차등가중).

    channels: {"title","lead","body"} 중 있는 것만. 없는 키는 빈 문자열.
    body 채널 매칭은 scoring.body_factor로 가중. 가속은 n_hits가 T 초과 시 볼록 가산.
    """
    penalties = ACTIVE_PENALTIES if penalties is None else penalties
    observe = OBSERVE_PENALTIES if observe is None else observe
    scoring = SCORING if scoring is None else scoring
    esc = scoring["escalation"]
    body_factor = scoring["body_factor"]

    hits: list[str] = []
    raw = 0.0
    n_hits = 0
    for p in penalties:
        c = _count_matches(p, _scope_text(p["scope"], channels))
        if c:
            factor = body_factor if p["scope"] == "body" else 1.0
            raw += p["weight"] * c * factor
            n_hits += c
            hits.extend([p["expr"]] * c)

    observe_hits: list[str] = []
    for p in observe:
        c = _count_matches(p, _scope_text(p["scope"], channels))
        if c:
            observe_hits.extend([p["expr"]] * c)

    points = raw + esc["step"] * max(0, n_hits - esc["T"])
    points = min(esc["cap"], points)
    score = max(FLOOR, BASELINE - points)
    return {"points": points, "hits": hits, "n_hits": n_hits,
            "observe_hits": observe_hits, "score": score}


def objectivity_score(article: dict, penalties=None, observe=None) -> dict:
    """호환 래퍼: 기사(title+summary)를 title/lead 채널로 채점."""
    channels = {"title": article.get("title", ""),
                "lead": article.get("summary", ""), "body": ""}
    return score_article(channels, penalties, observe)
```

- [ ] **Step 4: 기존 floor-clamp 테스트를 cap 값으로 갱신**

`tests/test_objectivity.py`의 `test_floor_clamp`(ScoreTest)와 `test_floor_clamp_with_weights`(GradedScoreTest)의 기대값을 수정:

```python
    def test_floor_clamp(self):
        # 가속·cap 도달: 반복 감점은 per-기사 cap(45)에서 멈춘다 → score 55
        text = "논란이 커지고 있다 " * 20
        art = {"title": text, "summary": ""}
        r = objectivity.objectivity_score(art)
        self.assertEqual(r["score"], 55)
```

```python
    def test_floor_clamp_with_weights(self):
        art = {"title": "반도체가 다 했네 " * 10, "summary": ""}  # cap 45에서 멈춤
        r = objectivity.objectivity_score(art, self.P, self.OBS)
        self.assertEqual(r["score"], 55)
```

- [ ] **Step 5: 테스트 실행 — 채점 관련 전부 통과**

Run: `python3 -m unittest tests.test_objectivity.ChannelScoreTest tests.test_objectivity.ScoreTest tests.test_objectivity.GradedScoreTest -v`
Expected: PASS (기존 phrase/weight 테스트는 n_hits≤3이라 값 불변; floor 2건은 55로 갱신됨)

- [ ] **Step 6: 커밋**

```bash
git add src/objectivity.py tests/test_objectivity.py
git commit -m "feat(objectivity): 채널별 채점 score_article(scope·가속·차등가중)"
```

---

## Task 4: 관찰축 — 인용 밀도(②) + 교차 이상치(④)

**Files:**
- Modify: `src/objectivity.py`
- Test: `tests/test_objectivity.py::ObservationAxisTest`(신규)

**Interfaces:**
- Consumes: `SCORING["attribution_markers"]`, `curate.normalize_title`, `score_article`.
- Produces:
  - `attribution_count(channels: dict, markers=None) -> int` = title+lead에서 귀속 표지 총 등장수.
  - `outlier_flags(articles: list[dict]) -> dict[str, bool]` = link→여부. 단독(같은 normalize_title 그룹의 distinct source가 1) & 감점 hit 동반이면 True.

- [ ] **Step 1: 실패 테스트 작성**

```python
class ObservationAxisTest(unittest.TestCase):
    def test_attribution_count(self):
        ch = {"title": "정부에 따르면 흑자", "lead": "관계자는 사실이라고 밝혔다", "body": ""}
        n = objectivity.attribution_count(
            ch, ["에 따르면", "라고 밝혔다", "고 밝혔다"])
        self.assertEqual(n, 2)   # '에 따르면' 1 + '고 밝혔다' 1

    def test_outlier_only_singleton_with_hit(self):
        arts = [
            # 단독 + 감점(아우성) → 이상치
            {"title": "혼자만 아우성", "summary": "", "link": "L1", "source": "A"},
            # 교차(같은 제목 다른 매체) + 감점 → 이상치 아님(단독 아님)
            {"title": "공동 논란이 커지고 있다", "summary": "", "link": "L2", "source": "B"},
            {"title": "공동 논란이 커지고 있다", "summary": "", "link": "L3", "source": "C"},
            # 단독 + 무감점 → 이상치 아님
            {"title": "혼자 깨끗한 기사", "summary": "", "link": "L4", "source": "D"},
        ]
        flags = objectivity.outlier_flags(arts)
        self.assertTrue(flags["L1"])
        self.assertFalse(flags["L2"])
        self.assertFalse(flags["L4"])
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python3 -m unittest tests.test_objectivity.ObservationAxisTest -v`
Expected: FAIL — `has no attribute 'attribution_count'`

- [ ] **Step 3: 구현 추가**

`curate` import에 `normalize_title` 추가:

```python
from curate import (  # 날짜창·출처목록·정규화 재사용(격리: 단방향 의존)
    SOURCES_FILE,
    in_date_window,
    load_priority_map,
    normalize_title,
)
```

`score_article` 아래에 추가:

```python
def attribution_count(channels: dict, markers=None) -> int:
    """제목+리드에서 귀속(출처 명시) 표지 등장수(② 중립 관찰축, 감점 아님)."""
    markers = SCORING["attribution_markers"] if markers is None else markers
    text = f"{channels.get('title','')} {channels.get('lead','')}"
    return sum(text.count(m) for m in markers)


def outlier_flags(articles: list[dict]) -> dict:
    """④ 교차 이상치: 단독(제목그룹 distinct source==1) & 감점 hit 동반 기사 표시."""
    groups: dict[str, set] = {}
    for a in articles:
        groups.setdefault(normalize_title(a.get("title", "")), set()).add(a.get("source", ""))
    flags: dict[str, bool] = {}
    for a in articles:
        norm = normalize_title(a.get("title", ""))
        singleton = len(groups[norm]) == 1
        has_hit = bool(score_article(
            {"title": a.get("title", ""), "lead": a.get("summary", ""), "body": ""})["hits"])
        flags[a.get("link", "")] = singleton and has_hit
    return flags
```

- [ ] **Step 4: 테스트 실행 — 통과**

Run: `python3 -m unittest tests.test_objectivity.ObservationAxisTest -v`
Expected: PASS (2개)

- [ ] **Step 5: 커밋**

```bash
git add src/objectivity.py tests/test_objectivity.py
git commit -m "feat(objectivity): 관찰축 인용 밀도·교차 이상치"
```

---

## Task 5: 밀도 집계 + media.json 스키마 + 리포트

**Files:**
- Modify: `src/objectivity.py`
- Test: `tests/test_objectivity.py::MediaDensityTest`(신규, 기존 `MediaAggregateTest` 대체)

**Interfaces:**
- Consumes: `score_article`, `attribution_count`, `outlier_flags`.
- Produces:
  - `update_media_scores(store, dated_articles, date) -> store` (개편): 매체별
    `{penalty_points_total, article_count, attribution_total, outlier_total, density_per_1000, count, last_seen}` 누적(멱등).
  - `process_date`가 `outlier_flags`로 이상치 반영, `save_article_report`가 밀도·관찰축 메모 저장.

- [ ] **Step 1: 기존 `MediaAggregateTest` 클래스를 삭제하고 `MediaDensityTest`로 교체**

`tests/test_objectivity.py`에서 `class MediaAggregateTest(...)` 전체(약 47~80행)를 아래로 교체:

```python
class MediaDensityTest(unittest.TestCase):
    def _empty(self):
        return {"media": {}, "processed_dates": []}

    def test_new_media_density(self):
        arts = [
            {"title": "깨끗한 기사", "summary": "", "source": "한국경제", "link": "L1"},
            {"title": "논란이 커지고 있다", "summary": "", "source": "한국경제", "link": "L2"},
        ]  # points 0 + 8 = 8, count 2 → 8/2*1000 = 4000
        store = objectivity.update_media_scores(self._empty(), arts, "2026-07-01")
        m = store["media"]["한국경제"]
        self.assertEqual(m["penalty_points_total"], 8)
        self.assertEqual(m["article_count"], 2)
        self.assertEqual(m["density_per_1000"], 4000.0)

    def test_density_accumulates_across_days(self):
        store = {"media": {"한국경제": {"penalty_points_total": 8, "article_count": 2,
                 "attribution_total": 0, "outlier_total": 0, "density_per_1000": 4000.0,
                 "count": 2, "last_seen": "2026-06-30"}},
                 "processed_dates": ["2026-06-30"]}
        arts = [{"title": "깨끗", "summary": "", "source": "한국경제", "link": "L3"}]
        store = objectivity.update_media_scores(store, arts, "2026-07-01")
        m = store["media"]["한국경제"]
        self.assertEqual(m["article_count"], 3)          # 2+1
        self.assertEqual(m["penalty_points_total"], 8)   # +0
        self.assertAlmostEqual(m["density_per_1000"], 8/3*1000)

    def test_idempotent_same_date(self):
        arts = [{"title": "깨끗", "summary": "", "source": "한국경제", "link": "L1"}]
        store = objectivity.update_media_scores(self._empty(), arts, "2026-07-01")
        snap = objectivity_snapshot(store)
        store = objectivity.update_media_scores(store, arts, "2026-07-01")
        self.assertEqual(objectivity_snapshot(store), snap)
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python3 -m unittest tests.test_objectivity.MediaDensityTest -v`
Expected: FAIL — `KeyError: 'penalty_points_total'`

- [ ] **Step 3: `update_media_scores` 개편**

기존 함수 본문(147~180행)을 교체:

```python
def update_media_scores(store: dict, dated_articles: list[dict], date: str) -> dict:
    """그날 기사들을 매체별로 채점해 감점 밀도로 누적한다(멱등)."""
    if date in store.get("processed_dates", []):
        return store

    flags = outlier_flags(dated_articles)
    agg: dict[str, dict] = {}
    for art in dated_articles:
        source = art.get("source", "")
        ch = {"title": art.get("title", ""), "lead": art.get("summary", ""), "body": ""}
        r = score_article(ch)
        a = agg.setdefault(source, {"points": 0.0, "count": 0, "attr": 0, "outlier": 0})
        a["points"] += r["points"]
        a["count"] += 1
        a["attr"] += attribution_count(ch)
        a["outlier"] += 1 if flags.get(art.get("link", "")) else 0

    media = store.setdefault("media", {})
    for source, a in agg.items():
        m = media.setdefault(source, {"penalty_points_total": 0.0, "article_count": 0,
                                      "attribution_total": 0, "outlier_total": 0,
                                      "density_per_1000": 0.0, "count": 0, "last_seen": date})
        m["penalty_points_total"] += a["points"]
        m["article_count"] += a["count"]
        m["attribution_total"] += a["attr"]
        m["outlier_total"] += a["outlier"]
        m["count"] = m["article_count"]
        m["last_seen"] = date
        m["density_per_1000"] = (m["penalty_points_total"] / m["article_count"] * 1000
                                 if m["article_count"] else 0.0)

    store.setdefault("processed_dates", []).append(date)
    return store
```

- [ ] **Step 4: `process_date`의 records에 관찰축 추가**

`process_date`의 records 루프(240~249행)를 교체:

```python
        flags = outlier_flags(articles)
        records = []
        for a in articles:
            ch = {"title": a.get("title", ""), "lead": a.get("summary", ""), "body": ""}
            r = score_article(ch)
            records.append({
                "source": a.get("source", ""),
                "category": a.get("category", ""),
                "title": a.get("title", ""),
                "link": a.get("link", ""),
                "score": r["score"],
                "points": r["points"],
                "hits": r["hits"],
                "attribution": attribution_count(ch),
                "outlier": bool(flags.get(a.get("link", ""))),
            })
        save_article_report(date, records)
```

- [ ] **Step 5: `save_article_report`에 밀도·관찰 요약 추가**

`save_article_report` 본문(197~208행)을 교체:

```python
def save_article_report(date: str, records: list[dict]) -> None:
    SCORES_DIR.mkdir(parents=True, exist_ok=True)
    penalized = [r for r in records if r.get("points", 0) > 0]
    total_points = sum(r.get("points", 0) for r in records)
    payload = {
        "date": date,
        "scored": len(records),
        "penalized_count": len(penalized),
        "total_points": total_points,
        "density_per_1000": (total_points / len(records) * 1000) if records else 0.0,
        "attribution_total": sum(r.get("attribution", 0) for r in records),
        "outlier_total": sum(1 for r in records if r.get("outlier")),
        "penalty_memo": penalty_memo(records),
        "articles": penalized,
    }
    with (SCORES_DIR / f"articles-{date}.json").open("w", encoding="utf-8") as f:
        json.dump(payload, f, ensure_ascii=False, indent=2)
```

- [ ] **Step 6: `main`의 출력을 밀도 기준으로 수정**

`main`의 출력 루프(287~289행)를 교체:

```python
    print(f"=== 객관성 감점 밀도 ({date}) — 낮을수록 객관적 ===")
    for source, m in sorted(store["media"].items(),
                            key=lambda kv: kv[1].get("density_per_1000", 0), reverse=True):
        print(f"  {m.get('density_per_1000', 0):7.1f}/1k · {source} "
              f"(표본 {m['count']}, 인용 {m.get('attribution_total',0)}, "
              f"이상치 {m.get('outlier_total',0)})")
```

- [ ] **Step 7: 기존 StoreIO/Report 테스트 갱신**

`StoreIOTest.test_article_report_saves_only_penalized`의 입력 record에 `points`를 추가하고 검증을 맞춘다:

```python
    def test_article_report_saves_only_penalized(self):
        with tempfile.TemporaryDirectory() as tmp:
            with patch.object(objectivity, "SCORES_DIR", Path(tmp)):
                objectivity.save_article_report("2026-07-01", [
                    {"source": "A", "category": "경제", "title": "논란이 커지고 있다",
                     "link": "L1", "score": 92, "points": 8, "hits": ["논란이 커지고 있다"]},
                ])
                data = json.loads((Path(tmp) / "articles-2026-07-01.json").read_text())
        self.assertEqual(data["penalized_count"], 1)
        self.assertEqual(data["articles"][0]["source"], "A")
```

- [ ] **Step 8: 전체 objectivity 테스트 실행**

Run: `python3 -m unittest tests.test_objectivity -v`
Expected: PASS (모든 클래스). 실패 시 관련 record/스키마 필드 확인.

- [ ] **Step 9: 커밋**

```bash
git add src/objectivity.py tests/test_objectivity.py
git commit -m "feat(objectivity): 감점 밀도 집계 + media.json 스키마 + 관찰축 리포트"
```

---

## Task 6: 트랙2 — 다이제스트 본문 감사 (digest_audit.py)

**Files:**
- Create: `src/digest_audit.py`
- Create: `tests/test_digest_audit.py`

**Interfaces:**
- Consumes: `selected/<date>.json`(categories→list[item], item에 `link`·`title`·`summary`), `objectivity.score_article`, 본문 추출 함수(주입 가능).
- Produces:
  - `audit_digest(date, fetch_body, selected_dir=None, scores_dir=None) -> dict`
    - `fetch_body(url) -> str`: 본문 텍스트(주입; 테스트는 가짜, 운영은 `extract.py` 사용).
    - 선별 각 기사에 대해 `channels={"title","lead"=summary,"body"=fetch_body(link)}`로 채점.
    - `scores/digest-audit-<date>.json` 저장: `{date, audited, penalized_count, items:[{source,title,link,score,points,hits}]}`. 반환값 동일 dict.
    - 트랙1 `media.json`은 **건드리지 않음**.

- [ ] **Step 1: 실패 테스트 작성**

`tests/test_digest_audit.py` 생성:

```python
"""트랙2 다이제스트 본문 감사 테스트(네트워크 미사용, 본문 추출은 주입)."""
import json
import os
import sys
import tempfile
import unittest
from pathlib import Path

sys.path.insert(0, os.path.join(os.path.dirname(__file__), "..", "src"))

import digest_audit  # noqa: E402


class AuditDigestTest(unittest.TestCase):
    def _write_selected(self, d, date):
        payload = {"date": date, "categories": {
            "경제": [{"title": "무역흑자 361억달러", "summary": "", "link": "L1", "source": "한국경제"}],
            "사회": [{"title": "구조 소식", "summary": "", "link": "L2", "source": "경향신문"}],
        }}
        (Path(d) / f"{date}.json").write_text(json.dumps(payload, ensure_ascii=False))

    def test_body_hit_is_scored_and_reported(self):
        bodies = {"L1": "여기저기 아우성이 커지고 있다", "L2": "차분한 사실 보도"}
        with tempfile.TemporaryDirectory() as tmp:
            sel = Path(tmp) / "selected"; sel.mkdir()
            sc = Path(tmp) / "scores"; sc.mkdir()
            self._write_selected(sel, "2026-07-01")
            result = digest_audit.audit_digest(
                "2026-07-01", fetch_body=lambda u: bodies.get(u, ""),
                selected_dir=sel, scores_dir=sc)
        # L1은 본문에 '아우성'(medium 8 × body_factor 0.5 = 4) → 감점
        self.assertEqual(result["audited"], 2)
        self.assertEqual(result["penalized_count"], 1)
        saved = json.loads((sc / "digest-audit-2026-07-01.json").read_text())
        self.assertEqual(saved["items"][0]["link"], "L1")

    def test_missing_body_falls_back_to_title_lead(self):
        with tempfile.TemporaryDirectory() as tmp:
            sel = Path(tmp) / "selected"; sel.mkdir()
            sc = Path(tmp) / "scores"; sc.mkdir()
            self._write_selected(sel, "2026-07-01")
            result = digest_audit.audit_digest(
                "2026-07-01", fetch_body=lambda u: "",   # 본문 없음
                selected_dir=sel, scores_dir=sc)
        self.assertEqual(result["audited"], 2)
        self.assertEqual(result["penalized_count"], 0)   # 제목·리드 깨끗


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python3 -m unittest tests.test_digest_audit -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'digest_audit'`

- [ ] **Step 3: `src/digest_audit.py` 구현**

```python
"""트랙2: 다이제스트(선별분) 본문 감사 — 제목+리드+본문 채점.

트랙1(매체 밀도)과 모집단·텍스트 범위가 달라 별도 리포트로 격리한다.
본문은 extract.py로 재추출(BeautifulSoup, AI 비용 0, 최대 ~16건/일).
매체 media.json은 건드리지 않는다(observe-only).
"""
from __future__ import annotations

import json
import sys
from pathlib import Path

import objectivity

ROOT = Path(__file__).resolve().parent.parent
SELECTED_DIR = ROOT / "selected"
SCORES_DIR = ROOT / "scores"


def _iter_selected(payload: dict):
    for _cat, items in payload.get("categories", {}).items():
        for it in items:
            yield it


def audit_digest(date: str, fetch_body, selected_dir: Path = None,
                 scores_dir: Path = None) -> dict:
    selected_dir = SELECTED_DIR if selected_dir is None else Path(selected_dir)
    scores_dir = SCORES_DIR if scores_dir is None else Path(scores_dir)
    payload = json.loads((selected_dir / f"{date}.json").read_text(encoding="utf-8"))

    items = []
    for it in _iter_selected(payload):
        body = fetch_body(it.get("link", "")) or ""
        ch = {"title": it.get("title", ""), "lead": it.get("summary", ""), "body": body}
        r = objectivity.score_article(ch)
        items.append({"source": it.get("source", ""), "title": it.get("title", ""),
                      "link": it.get("link", ""), "score": r["score"],
                      "points": r["points"], "hits": r["hits"]})

    penalized = [i for i in items if i["points"] > 0]
    result = {"date": date, "audited": len(items),
              "penalized_count": len(penalized), "items": penalized}
    scores_dir.mkdir(parents=True, exist_ok=True)
    (scores_dir / f"digest-audit-{date}.json").write_text(
        json.dumps(result, ensure_ascii=False, indent=2), encoding="utf-8")
    return result


def _extract_body(url: str) -> str:
    """운영용 본문 추출(extract.py 재사용). 실패 시 빈 문자열."""
    try:
        import extract
        return extract.extract_body(url) or ""
    except Exception as exc:  # 추출 실패는 감사 스킵(치명적 아님)
        print(f"경고: 본문 추출 실패 {url} — {exc}", file=sys.stderr)
        return ""


def main() -> int:
    from datetime import datetime, timedelta, timezone
    kst = timezone(timedelta(hours=9))
    date = sys.argv[1] if len(sys.argv) > 1 else datetime.now(kst).strftime("%Y-%m-%d")
    result = audit_digest(date, fetch_body=_extract_body)
    print(f"=== 다이제스트 본문 감사 ({date}) ===")
    print(f"  감사 {result['audited']}건, 감점 {result['penalized_count']}건")
    for i in result["items"]:
        print(f"  -{i['points']:.1f} · {i['source']} · {i['title'][:30]} {i['hits']}")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: `_extract_body`가 참조하는 함수명 확인**

Run: `grep -n "^def " src/extract.py`
확인됨: 공개 함수는 `extract_body(url) -> str | None`. `_extract_body`는 이를 호출한다(계획 반영 완료). 테스트는 `fetch_body` 주입이라 영향 없음.

- [ ] **Step 5: 테스트 실행 — 통과**

Run: `python3 -m unittest tests.test_digest_audit -v`
Expected: PASS (2개)

- [ ] **Step 6: 커밋**

```bash
git add src/digest_audit.py tests/test_digest_audit.py
git commit -m "feat(objectivity): 트랙2 다이제스트 본문 감사(격리 모듈)"
```

---

## Task 7: 스킬 출력 갱신 (objectivity-score / daily-briefing)

**Files:**
- Modify: `.claude/skills/objectivity-score/SKILL.md`
- Modify: `.claude/skills/daily-briefing/SKILL.md`

**Interfaces:**
- Consumes: 새 `media.json`(density_per_1000, attribution_total, outlier_total), `articles-<date>.json`, `digest-audit-<date>.json`.

- [ ] **Step 1: objectivity-score 스킬의 출력 절차 수정**

`.claude/skills/objectivity-score/SKILL.md`에서 점수 렌더링 부분을 찾아, "0~100 점수" 표기를 아래 밀도 표기로 교체(정확 문구는 기존 표현에 맞춰 최소 수정):

- 누적 표: `density_per_1000`(낮을수록 객관적) 오름차순 정렬, 컬럼 = 매체 · 감점밀도(/1k) · 표본 · 인용 · 이상치.
- 당일 메모: `articles-<date>.json`의 `total_points`·`density_per_1000`·`penalty_memo.by_expr`·`by_source` 렌더.
- 트랙2: `digest-audit-<date>.json`이 있으면 "본문 감사: 감사 N건, 감점 M건" 요약 추가.
- observe-only·멱등·`scores/` 커밋 확인 주의는 유지.

- [ ] **Step 2: daily-briefing 스킬의 ③ 우선순위 점수 섹션 수정**

`.claude/skills/daily-briefing/SKILL.md`의 "우선순위 점수" 항목을 밀도 기준(낮을수록 객관적)으로 바꾸고, 관찰축(인용·이상치)과 트랙2 감사 요약을 포함하도록 문구 갱신.

- [ ] **Step 3: 스킬 문서 정합성 확인(문서라 테스트 없음)**

두 SKILL.md에 "0~100", "EWMA", "높을수록 좋음" 잔재가 없는지 grep:

Run: `grep -n "0~100\|EWMA\|높을수록" .claude/skills/objectivity-score/SKILL.md .claude/skills/daily-briefing/SKILL.md`
Expected: 밀도 전환과 모순되는 잔재 없음(있으면 수정).

- [ ] **Step 4: 커밋**

```bash
git add .claude/skills/objectivity-score/SKILL.md .claude/skills/daily-briefing/SKILL.md
git commit -m "docs(skills): 객관성 출력 밀도·관찰축·본문감사로 갱신"
```

---

## Task 8: backfill 리셋 + 변별력 검증

**Files:**
- Modify: `scores/media.json`, `scores/articles-*.json`(재생성 산출물)

**Interfaces:**
- Consumes: 위 전 태스크. 실제 `raw/*.json`으로 재구축.

- [ ] **Step 1: 전체 테스트 스위트 확인**

Run: `python3 -m unittest discover -s tests -v`
Expected: PASS (objectivity·digest_audit·curate·fallback·notify·failure_log 전부)

- [ ] **Step 2: media.json 리셋 재구축(backfill)**

Run: `python3 src/objectivity.py --backfill`
Expected: "백필 완료: N일 처리, 매체 M곳". `scores/media.json`이 새 스키마(density_per_1000)로 재생성.

- [ ] **Step 3: 변별력 확인**

Run: `python3 -c "import json;d=json.load(open('scores/media.json'));[print(f'{m[\"density_per_1000\"]:7.1f}/1k  {s}  (표본 {m[\"article_count\"]}, 인용 {m[\"attribution_total\"]}, 이상치 {m[\"outlier_total\"]})') for s,m in sorted(d['media'].items(), key=lambda kv: kv[1]['density_per_1000'])]"`
Expected: 매체별 density_per_1000이 **서로 벌어짐**(현 99.8~100 압축 해소). 성공 기준 1 충족. (값 자체는 시드 상수 기준 — 캘리브레이션은 후속)

- [ ] **Step 4: observe-only 불변 확인**

Run: `git status --porcelain src/curate.py src/summarize.py run.sh .github/workflows/daily.yml`
Expected: 출력 없음(미변경). 성공 기준 3 충족.

- [ ] **Step 5: 산출물 커밋**

```bash
git add scores/
git commit -m "chore(scores): 감점 밀도 스키마로 media.json 리셋 재구축"
```

---

## Self-Review 결과

- **스펙 커버리지:** ①구조(T1 Step2)·②인용(T4)·③어휘(T1 Step3)·④이상치(T4)·밀도전환(T5)·두 트랙(T5/T6)·차등가중·가속(T3)·스킬(T7)·backfill 검증(T8) 모두 태스크로 매핑됨.
- **스펙 정정:** 트랙2는 `cache/summaries.json`(요약 불릿)이 아니라 `extract.py` 본문 재추출을 사용(정확). 계획에 반영.
- **타입 일관성:** `score_article`가 반환하는 `points/hits/n_hits/observe_hits/score` 키를 T3~T8에서 일관 사용. `media.json` 필드(`penalty_points_total/article_count/density_per_1000/attribution_total/outlier_total`)를 T5~T8에서 일관 사용.
- **미정 확인:** T6 Step4에서 `extract.py` 실제 함수명 확인 절차 포함(주입 테스트라 코어 로직엔 영향 없음).
